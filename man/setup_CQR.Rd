% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/260_PredictionRegion.R
\name{setup_CQR}
\alias{setup_CQR}
\title{Setup Conformalized Quantile Regression}
\usage{
setup_CQR(alpha = 0.1)
}
\arguments{
\item{alpha}{Numeric (0, 1): Miscoverage rate. Also sets the quantile levels
queried, \code{alpha/2} and \code{1 - alpha/2}.}
}
\value{
\code{CQRConfig} object.
}
\description{
Configure conformalized quantile regression for \link{conformal}.
}
\details{
Split conformal adds and subtracts one number, so every case gets the same
width whether the model is confident or not. CQR (Romano, Patterson and
Candes, 2019) starts from the model's own \code{alpha/2} and \code{1 - alpha/2}
quantiles and conformalizes the pair with the score
\code{max(qlo - y, y - qhi)}, which widens or narrows both ends by one calibrated
amount. Adaptive width, same finite-sample guarantee.

\strong{It needs a backend that answers a quantile query from the model already
fitted.} Today that is Ranger trained with \code{setup_Ranger(quantreg = TRUE)}:
a quantile regression forest keeps the training outcomes at its terminal
nodes, so one fit answers every level. A gradient booster on the \code{quantile}
objective targets the single level it was trained for, so a pair of bounds
would be a pair of models -- a training configuration, not something
\code{conformal()} can do to a model it is handed.

\strong{It always needs \code{calibration} data.} A \code{Supervised} stores its test
predictions but not its test features, and CQR must query the model for
quantiles at the calibration cases, so the stored-split default does not
apply here.
}
\examples{
setup_CQR()
setup_CQR(alpha = 0.05)
}
\author{
EDG
}
