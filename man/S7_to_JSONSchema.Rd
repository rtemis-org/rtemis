% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_Props.R
\name{S7_to_JSONSchema}
\alias{S7_to_JSONSchema}
\title{Convert an S7 class built with \verb{prop_*} factories to a JSON Schema}
\usage{
S7_to_JSONSchema(
  x,
  id,
  title = NULL,
  description = "",
  base = NULL,
  required = NULL,
  record = FALSE,
  provenance_url = NULL,
  extra = NULL,
  refs = NULL,
  closed = TRUE,
  instance_schema_url = NULL
)
}
\arguments{
\item{x}{S7 class (e.g. \code{LightRFHyperparameters}).}

\item{id}{Character: Schema \verb{$id} URL
(e.g. "https://schema.rtemis.org/hyperparameters/lightrf/v1/schema.json").}

\item{title}{Character: Schema title. Defaults to the class name.}

\item{description}{Character: Schema description. If empty, the
"description" keyword is omitted from the schema.}

\item{base}{S7 class or NULL: The family base class, whose inherited
properties are machinery (\code{tuned}, \code{resampled}, the computed payload list)
rather than config, and are omitted. NULL for a flat config that has no
family base.}

\item{required}{Character: Names of required properties. Default NULL: all
optional, so omitted fields fall back to their \verb{setup_*} defaults on read
(matching \link{write_config}'s compaction). Ignored when \code{record} is TRUE.}

\item{record}{Logical: If TRUE, emit the \strong{record} form of the schema: the
same properties, but every one required. A record states what a run
actually used, so nothing in it may fall back to a reader's defaults — an
unset value is written as an explicit \code{null} rather than omitted. The
difference between an input schema and a record schema is exactly this;
membership is identical.}

\item{provenance_url}{Character or NULL: If set (and \code{record} is TRUE), adds
a required \code{provenance} property \verb{$ref}ing that schema. Only a top-level
record carries it; a nested one inherits its parent's.}

\item{extra}{Named list merged into the schema after generation, for
cross-field constraints that are not per-property (e.g. an \code{allOf} of
if/then clauses for kernel-specific SVM hyperparameters).}

\item{refs}{Named character: Properties holding a nested config object,
mapped to the \verb{$id} of the schema for that config. Each emits a \verb{$ref} (or
\verb{oneOf: [null, $ref]} when the property accepts NULL, detected from its
S7 union), instead of requiring a \code{PropertySpec}. Names must match
existing properties.}

\item{closed}{Logical: If TRUE (default) the schema sets
\code{additionalProperties: false}. Pass FALSE for leaves composed into a
top-level-mode dispatcher, which enforces strictness with
\code{unevaluatedProperties} instead (see \link{S7_dispatcher_JSONSchema}).}

\item{instance_schema_url}{Character or NULL: If set, adds a \verb{$schema}
const property (instances self-identify, as in the config families).}
}
\value{
Named list: the JSON Schema. Serialize with \link{write_JSONSchema}.
}
\description{
Walks the class's properties, reads each attached \code{PropertySpec}, and
assembles a draft 2020-12 JSON Schema. Which properties take part is decided
by their declared role (see \code{prop_role()}), not by a list kept here:
\code{"config"} properties are generated from their spec, \code{"state"} properties are
dropped, and \code{"external"} properties must be supplied by \code{extra} — asserted
after the merge, so a forgotten fragment cannot silently drop a key from the
published contract. A spec-less property with no role is an error, so a class
that drifts from the factory vocabulary fails loudly instead of emitting a
wrong schema.
}
\examples{
\dontrun{
schema <- S7_to_JSONSchema(
  LightRFHyperparameters,
  id = "https://schema.rtemis.org/hyperparameters/lightrf/v1/schema.json",
  base = Hyperparameters
)
}
}
\author{
EDG
}
