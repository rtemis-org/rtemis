% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/260_PredictionRegion.R
\name{setup_SplitConformal}
\alias{setup_SplitConformal}
\title{Setup Split Conformal Prediction}
\usage{
setup_SplitConformal(alpha = 0.1, score = NULL, seed = NULL)
}
\arguments{
\item{alpha}{Numeric (0, 1): Miscoverage rate. The region covers with
probability at least \code{1 - alpha}.}

\item{score}{Optional Character \{"absolute", "LAC", "APS"\}: Nonconformity
score. NULL resolves to "absolute" for a regression and "APS" for a
classification.}

\item{seed}{Optional Integer [0, Inf): Random seed for the APS draw. NULL
draws one and records it.}
}
\value{
\code{SplitConformalConfig} object.
}
\description{
Configure split conformal prediction for \link{conformal}.
}
\details{
The reference construction: score every calibration case, take \code{q} to be the
score at rank \code{ceiling((n + 1) * (1 - alpha))} from the bottom, and return
every outcome whose score would not exceed it. Coverage is at least
\code{1 - alpha}, and needs only that the calibration cases and the test case be
exchangeable.

\strong{\code{alpha} has a floor set by the calibration count.} A finite region needs
\code{ceiling((n + 1) * (1 - alpha)) <= n}, so \code{alpha = 0.05} needs at least 19
calibration cases and \code{alpha = 0.01} at least 99. \code{conformal()} aborts naming
the count rather than returning an infinite region.

\strong{\code{score} is the real choice, and only for a classification.} A regression
has one score, \code{abs(y - yhat)}, which is what NULL resolves to.
\itemize{
\item \code{"APS"}, the default, accumulates the predicted probabilities from the most
probable class down to the true one. Larger sets, markedly better coverage
on the hard cases -- which is the reason to want a set at all.
\item \code{"LAC"} scores \code{1 - p(y)}. It produces the smallest sets attaining valid
\emph{marginal} coverage, and it buys them by under-covering exactly the cases
where the model is unsure. Choose it when small sets are the point and the
trade is understood.
}

\strong{APS is randomized} (Romano, Sesia and Candes, 2020): the true label's own
probability enters its score multiplied by a uniform draw, one per case. That
is not a refinement. Without it, every case whose true label ranks last
scores exactly 1, so a model erring on more than \code{alpha} of its cases puts
the threshold at 1 and every set holds every label -- valid coverage carrying
no information. \code{seed} keeps the draw reproducible: left NULL, one is drawn
and recorded on the returned config, so an unseeded region can still be
reproduced from what it reports. \code{"LAC"} draws nothing and ignores it.
}
\examples{
setup_SplitConformal()
setup_SplitConformal(alpha = 0.05)
setup_SplitConformal(score = "LAC")
}
\author{
EDG
}
