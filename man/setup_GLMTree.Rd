% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_GLMTree}
\alias{setup_GLMTree}
\title{Setup GLMTree Hyperparameters}
\usage{
setup_GLMTree(
  alpha = 0.05,
  minsize = NULL,
  maxdepth = NULL,
  mtry = NULL,
  trim = 0.1,
  prune = NULL,
  numsplit = "left",
  catsplit = "binary",
  ordinal = "chisq",
  ifw = FALSE,
  regressors = NULL,
  partitioning_variables = NULL,
  bonferroni = TRUE,
  breakties = FALSE,
  restart = TRUE,
  dfsplit = TRUE,
  vcov = "opg",
  nrep = 10000L
)
}
\arguments{
\item{alpha}{(Tunable) Numeric (0, 1]: Significance level a parameter-instability test must reach before a node is split.}

\item{minsize}{(Tunable) Optional Integer [1, Inf): Fewest cases a node may hold. NULL uses ten times the number of parameters in a leaf's model.}

\item{maxdepth}{(Tunable) Optional Integer [1, Inf): Deepest the tree may grow. NULL is unlimited.}

\item{mtry}{(Tunable) Optional Integer [1, Inf): Partitioning variables sampled at each node. NULL tests every one.}

\item{trim}{(Tunable) Numeric [0, Inf): Trimming for the split-point search; a fraction of cases below 1, a count at or above it.}

\item{prune}{(Tunable) Optional Character \{"AIC", "BIC"\}: Post-prune the grown tree by an information criterion. NULL keeps every split.}

\item{numsplit}{(Tunable) Character \{"left", "center"\}: Where a numeric split is placed.}

\item{catsplit}{(Tunable) Character \{"binary", "multiway"\}: How a categorical variable is split.}

\item{ordinal}{(Tunable) Character \{"chisq", "max", "L2"\}: Test statistic for an ordered partitioning variable.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}

\item{regressors}{Optional Character vector: Features entering the linear model in each leaf. NULL uses every feature.}

\item{partitioning_variables}{Optional Character vector: Features the tree may split on. NULL uses every feature.}

\item{bonferroni}{Logical: If TRUE, Bonferroni-adjust the test p-values for the number of partitioning variables.}

\item{breakties}{Logical: If TRUE, break ties in numeric partitioning variables by adding noise.}

\item{restart}{Logical: If TRUE, restart each leaf's model from scratch rather than from its parent's fit.}

\item{dfsplit}{Logical: If TRUE, count each split against the degrees of freedom when pruning.}

\item{vcov}{Character \{"opg", "info", "sandwich"\}: Covariance estimator the instability test is built on.}

\item{nrep}{Integer [1, Inf): Simulation replicates for the p-value of an ordinal split statistic.}
}
\value{
GLMTreeHyperparameters object.
}
\description{
Setup hyperparameters for GLMTree: model-based recursive partitioning, a tree
carrying a generalized linear model in each leaf.
}
\details{
Where a decision tree predicts a constant in each leaf, a GLMTree fits a
regression there, so the model reads as "in this subgroup, these are the
coefficients". \code{partykit::lmtree()} fits a regression, \code{partykit::glmtree()}
a binary logistic one; rtemis selects between them from the outcome.
}
\section{How it splits}{

Not by loss reduction. A node is split when a \strong{parameter-instability test}
says its model's coefficients are not constant across a candidate
partitioning variable -- a structural change test, at significance level
\code{alpha}. That is the method's distinguishing feature and the source of both
its strengths and its limits:

\describe{
\item{It has a hypothesis-testing foundation}{Splits carry p-values, and
\code{alpha} is a meaningful dial rather than a tuning knob without units.}
\item{It sees a slope change with no shift in the mean}{Which a tree
splitting on the response cannot: there is nothing in the mean to find.}
\item{It needs statistical power}{With few cases, many partitioning
variables, or both, the tests do not reach \code{alpha} and the tree does not
split at all -- returning a single global model.}
}
}

\section{When it will not split, check \code{minsize} before \code{alpha}}{

The default \code{minsize} is \strong{ten times the number of parameters in a leaf's
model}, and that scales with the number of regressors. A split has to leave
\code{minsize} cases on each side, so with \code{p} regressors no split is possible
below \code{n = 20 * (p + 1)} -- 420 cases at 20 regressors, whatever \code{alpha}
says. A tree that returns one leaf on a wide design has usually hit this,
not the significance level, and raising \code{alpha} will not move it. Set
\code{minsize} directly, or narrow \code{regressors}.

\code{regressors} and \code{partitioning_variables} are separable, and the separation
is the point: the model in each leaf need not read the variables the tree
splits on. Both default to every feature.
}

\section{Interpretability}{

Every leaf carries a full model over \code{regressors} -- there is no selection or
shrinkage, so a leaf holds one coefficient per regressor whether or not it
earned one. Restrict \code{regressors} to keep that number down.
}

\section{What it does not provide}{

No variable-importance measure: \code{partykit} has one for \code{cforest} and for
constant-fit trees, and none for a model-based tree. \code{get_varimp()} therefore
returns NULL, and \code{get_varimp(explain(mod, newdata))} is the route to a
per-feature measure.
}

\examples{
glmtree_hyperparams <- setup_GLMTree(alpha = 0.01)
glmtree_hyperparams
}
\references{
Zeileis A, Hothorn T, Hornik K (2008). Model-Based Recursive Partitioning.
\emph{Journal of Computational and Graphical Statistics}, 17(2), 492-514.
\doi{10.1198/106186008X319331}
}
\author{
EDG
}
