% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_init.R
\name{preprocess}
\alias{preprocess}
\title{Preprocess Data}
\usage{
preprocess(
  x,
  config,
  dat_validation = NULL,
  dat_test = NULL,
  verbosity = 1L,
  ...
)
}
\arguments{
\item{x}{data.frame or data.table: Training set data to preprocess.}

\item{config}{\code{PreprocessorConfig}: Preprocessing configuration created by
\link{setup_Preprocessor}. May be omitted, in which case \link{setup_Preprocessor} arguments are
passed directly via \code{...}.}

\item{dat_validation}{Optional data.frame or data.table: Validation set data. Preprocessed
using the values learned from the training set data.}

\item{dat_test}{Optional data.frame or data.table: Test set data. Preprocessed using the
values learned from the training set data.}

\item{verbosity}{Integer: Verbosity level.}

\item{...}{\link{setup_Preprocessor} arguments: Only used when \code{config} is not provided.}
}
\value{
\code{Preprocessor} object.
}
\description{
Preprocess data for analysis and visualization.
}
\details{
\code{preprocess()} preprocesses training data and learns any data-dependent values (e.g. scale
centers and coefficients, one-hot levels). Optional \code{dat_validation} and \code{dat_test} data are
preprocessed using the values learned from the training data. To apply a trained
\code{Preprocessor} to new data, use \link{apply_preprocessor}.

For interactive use, \code{config} may be omitted and \link{setup_Preprocessor} arguments passed
directly instead, e.g. \code{preprocess(x, scale = TRUE, center = TRUE)}. At least one
preprocessing parameter must be specified: \code{preprocess(x)} is an error.
}
\examples{
# Setup a `Preprocessor`: this outputs a `PreprocessorConfig` object.
prp <- setup_Preprocessor(remove_duplicates = TRUE, scale = TRUE, center = TRUE)

# Includes a long list of parameters
prp

# Resample iris to get train and test data
res <- resample(iris, setup_Resampler(seed = 2026))
iris_train <- iris[res[[1]], ]
iris_test <- iris[-res[[1]], ]

# Preprocess training data
iris_pre <- preprocess(iris_train, prp)

# Alternatively, for interactive use, pass `setup_Preprocessor()` arguments directly
iris_pre <- preprocess(iris_train, remove_duplicates = TRUE, scale = TRUE, center = TRUE)

# Access preprocessed training data with `preprocessed()`
preprocessed(iris_pre)

# Apply the same preprocessing to test data with `apply_preprocessor()`,
# which returns the preprocessed data directly.
# The scale and center values learned from the training data will be used.
iris_test_pre <- apply_preprocessor(iris_pre, iris_test)
}
\seealso{
\link{apply_preprocessor}, \link{setup_Preprocessor}
}
\author{
EDG
}
