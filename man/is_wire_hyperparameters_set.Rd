% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/075_HyperparametersSet.R
\name{is_wire_hyperparameters_set}
\alias{is_wire_hyperparameters_set}
\title{Is this parsed JSON a set of hyperparameters?}
\usage{
is_wire_hyperparameters_set(x)
}
\arguments{
\item{x}{Value to test, as parsed from JSON.}
}
\value{
Logical.
}
\description{
The tag \code{serializable_props()} writes, \code{{"variants": {...}}}. An exact-names
test, as \code{is_wire_candidates()} uses: a reader tells a set from a single
configuration with no reference to what the property declares, and a document
carrying anything beside \code{variants} is not one.
}
\details{
Exported for \code{rtemis.server}, which needs the same discriminator one layer
earlier: its \code{train} handler has to know whether a payload names its learner
at the top level or inside each variant, and that question has to be answered
the same way there as it is in \code{.list_to_SuperConfig()}. Two spellings of one
rule is how a config becomes submittable on one path and not the other.
}
\examples{
is_wire_hyperparameters_set(list(variants = list()))
}
\author{
EDG
}
\keyword{internal}
