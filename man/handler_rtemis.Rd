% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handler_rtemis.R
\name{handler_rtemis}
\alias{handler_rtemis}
\title{progressr handler rendering through the rtemis progress system}
\usage{
handler_rtemis(
  label = "Progress",
  kind = "progress",
  verbosity = NULL,
  output_type = NULL,
  enable = TRUE,
  target = "terminal",
  ...
)
}
\arguments{
\item{label}{Character: Display label for the progress node.}

\item{kind}{Character: Node kind forwarded in the sink envelope.}

\item{verbosity}{Integer or NULL: Overrides \code{rtemis.core::get_verbosity()}
when supplied. Gates only console rendering, never sink events.}

\item{output_type}{Character or NULL: \code{"ansi"}, \code{"html"}, or \code{"plain"};
resolved via \code{\link[rtemis.core:get_output_type]{rtemis.core::get_output_type()}} when NULL.}

\item{enable}{Logical: If TRUE, handler is active (see Details).}

\item{target}{Character: progressr handler target.}

\item{...}{Additional arguments passed to
\code{\link[progressr:make_progression_handler]{progressr::make_progression_handler()}}.}
}
\value{
A \code{progression_handler} function, for use with
\code{\link[progressr:with_progress]{progressr::with_progress()}} or \code{\link[progressr:handlers]{progressr::handlers()}}.
}
\description{
A \code{\link[progressr:make_progression_handler]{progressr::make_progression_handler()}} backend that maps \code{progression}
conditions onto \code{\link[rtemis.core:progress_begin]{rtemis.core::progress_begin()}} /
\code{\link[rtemis.core:progress_update]{rtemis.core::progress_update()}} / \code{\link[rtemis.core:progress_end]{rtemis.core::progress_end()}}. This is
how parallel loops report progress in rtemis: workers signal progressions
(e.g. via \code{progressr::progressor()}), the future framework relays them to
the main session, and this handler renders them - so parallel tuning gets
the same breadcrumb status line, nests correctly under any active
sequential progress (e.g. outer resamples), and emits structured
\code{level = "progress"} envelopes when a message sink is set (see
\code{\link[rtemis.core:set_msg_sink]{rtemis.core::set_msg_sink()}}).
}
\details{
Used internally by \code{tune_GridSearch()} around its future backend, wrapped
as \code{progressr::with_progress(..., handlers = handler_rtemis(...))}. It can
also be registered globally for arbitrary progressr-instrumented code:
\code{progressr::handlers(handler_rtemis())}.

\code{enable} defaults to \code{TRUE} (unlike most progressr handlers, which default
to interactive sessions only) because the rtemis progress system handles
non-interactive output itself and must emit sink envelopes regardless of
the display; console rendering is gated by \code{verbosity} downstream. Note
that \code{\link[progressr:with_progress]{progressr::with_progress()}} additionally gates delivery on the
global \code{progressr.enable} option (FALSE in non-interactive sessions), so
non-interactive callers should also pass \code{enable = TRUE} to
\code{with_progress()} itself, as \code{tune_GridSearch()} does.

Update frequency is bounded by when the parallel framework relays
conditions (typically as chunks resolve) and by the rtemis.core throttle
(\code{options(rtemis.progress_throttle = )}).
}
\examples{
\dontrun{
progressr::with_progress(
  {
    p <- progressr::progressor(steps = 5L)
    for (i in 1:5) {
      Sys.sleep(0.1)
      p()
    }
  },
  handlers = handler_rtemis(label = "Working"),
  enable = TRUE
)
}
}
\author{
EDG
}
