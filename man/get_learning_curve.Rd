% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/030_init.R
\name{get_learning_curve}
\alias{get_learning_curve}
\title{Learning curve of a fitted model}
\usage{
get_learning_curve(x, ...)
}
\arguments{
\item{x}{\code{Supervised} object.}

\item{...}{Additional arguments passed to methods.}
}
\value{
data.frame with columns \code{iteration}, \code{loss_training} and
\code{loss_validation}, carrying attributes \code{unit} and \code{selected}; or NULL.
}
\description{
The loss recorded at every step of training, as data rather than a picture:
one row per step, with the training and validation loss where the algorithm
records them.

Returns NULL for an algorithm that records no learning curve, so it is safe
to call on any model.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
dat <- set_outcome(iris[, 1:4], "Sepal.Length")
mod <- train(
  dat[1:100, ],
  dat_validation = dat[101:150, ],
  hyperparameters = setup_LINAD(max_leaves = 12L)
)
get_learning_curve(mod)
\dontshow{\}) # examplesIf}
}
\seealso{
\link{plot_learning}, which draws it
}
\author{
EDG
}
