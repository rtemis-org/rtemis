% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decomp_metrics.R
\name{decomp_metrics}
\alias{decomp_metrics}
\title{Decomposition Metrics}
\usage{
decomp_metrics(decom, x, new_data = NULL, verbosity = 1L)
}
\arguments{
\item{decom}{\code{Decomposition} object.}

\item{x}{Tabular data (data.frame, data.table, or tibble): The data \code{decom}
was fitted on.}

\item{new_data}{Optional tabular data: Data the fit never saw. Its columns
must match those \code{decom} was fitted on.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{DecompositionMetrics} object.
}
\description{
Quantify a fitted decomposition: how much of the data its components
reconstruct, how redundant they are, and -- given data the fit never saw --
whether it generalizes.
}
\details{
Which metrics are computed follows from the algorithm's traits; see
\link{applicable_metrics}. A metric its algorithm cannot support is \code{NA}.

\code{decomp()} already stores this on its result for the data it was fitted on,
so calling this without \code{new_data} recomputes what \code{decom@metrics} holds. The
reason to call it is \code{new_data}: \code{reconstruction_gap} is the difference
between out-of-sample and in-sample relative reconstruction error, and it is
the signal that a decomposition has fitted its training cases rather than
their structure.

Reconstruction is measured in the units of the data as it was passed to
\code{decomp()}, so the numbers are comparable across algorithms and across
configurations of one algorithm. One caveat: ICA with \code{row_norm = TRUE}
standardizes each case across features, and inverting that needs each case's
own mean and standard deviation, taken from the data being reconstructed.
Its reconstruction therefore draws on information the components do not
carry, and its error is not comparable with an algorithm that reconstructs
from components alone.
}
\examples{
x <- exc(iris, "Species")
iris_pca <- decomp(x, algorithm = "PCA", verbosity = 0L)
decomp_metrics(iris_pca, x, verbosity = 0L)
}
\author{
EDG
}
