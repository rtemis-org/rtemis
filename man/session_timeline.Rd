% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/180_Session.R
\name{session_timeline}
\alias{session_timeline}
\title{Session Execution Timeline Table}
\usage{
session_timeline(session)
}
\arguments{
\item{session}{\code{SupervisedSession}: Session object, e.g. \code{model@session}.}
}
\value{
\code{data.table} with one row per node in depth-first order and columns:
\itemize{
\item \code{label} Character: Unique display label, indented two spaces per tree
depth (duplicates disambiguated with \code{" #<n>"} suffixes).
\item \code{start} Numeric: Node start, milliseconds from session start.
\item \code{end} Numeric: Node end, milliseconds from session start.
\item \code{kind} Character: Node kind (e.g. \code{"tune"}, \code{"grid_cell"}, \code{"train_alg"}).
\item \code{status} Character \{"ok", "error", "aborted", "running"\}: Node status.
\item \code{failed} Logical: \code{TRUE} when \code{status} is \code{"error"} or \code{"aborted"}.
\item \code{tip} Character: Preformatted tooltip text (\code{"name -- duration [status]"}).
}
}
\description{
Flatten the execution graph captured in a \code{SupervisedSession} (from
\code{\link[=train]{train()}}) into a timeline (Gantt) table: one row per recorded node, ordered
as a depth-first walk of the execution tree, with start/end offsets in
milliseconds from session start.
}
\details{
This is the single source of the tree-walk, labeling, and status logic for
timeline renderers. \code{rtemis.server} consumes it for the \code{job.result}
\code{session} slice, which the rtemislive web UI draws; pair it with
\code{session_kind_colors()} to color bars by node kind.

Nodes that never closed (status \code{"running"} or \code{"aborted"} with no end time)
are drawn up to the session finish time when known, else to the latest
recorded node start.
}
\examples{
mod <- train(iris, hyperparameters = setup_CART())
# One row per recorded node, depth-first: the run, then the steps under it.
session_timeline(mod@session)
}
\author{
EDG
}
