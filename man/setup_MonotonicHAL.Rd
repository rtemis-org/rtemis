% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_MonotonicHAL}
\alias{setup_MonotonicHAL}
\title{Setup MonotonicHAL Hyperparameters}
\usage{
setup_MonotonicHAL(
  smoothness_orders = 1L,
  reduce_basis = NULL,
  num_knots = NULL,
  penalized = TRUE,
  use_min = TRUE,
  nfolds = 10L,
  seed = NULL,
  ifw = FALSE
)
}
\arguments{
\item{smoothness_orders}{(Tunable) Integer [0, 1]: Smoothness of the basis functions: 0 fits zero-order indicators and yields a step function, 1 fits piecewise linear splines and yields a continuous one.}

\item{reduce_basis}{(Tunable) Optional Numeric (0, 1]: Minimum proportion of cases a basis function must be non-zero in to be kept. Applies only when \code{smoothness_orders} is 0; a search that also covers higher orders drops it from those grid cells.}

\item{num_knots}{Optional Integer [1, Inf): Number of knots spanning each feature. NULL generates them from smoothness_orders.}

\item{penalized}{Logical: If TRUE, apply the lasso penalty to the basis functions; if FALSE, remove it.}

\item{use_min}{Logical: If TRUE, select \code{lambda.min} from the internal cross-validation; if FALSE, the more heavily penalized \code{lambda.1se}.}

\item{nfolds}{Integer [3, Inf): Largest number of folds of the internal cross-validation that selects lambda.}

\item{seed}{Optional Integer: Random seed for the internal cross-validation's fold assignment. NULL leaves it drawn from the ambient RNG.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
MonotonicHALHyperparameters object.
}
\description{
Setup hyperparameters for monotonic Highly Adaptive Lasso training.
}
\details{
A Highly Adaptive Lasso restricted to additive, monotonic non-decreasing
fits. \link[hal9001:fit_hal]{hal9001::fit_hal} is given a formula that constrains every basis
function's coefficient to be non-negative, and the interaction degree is
fixed at 1. Regression uses the gaussian family, binary classification the
binomial one; \code{hal9001} has no multinomial family, so multiclass
classification is not supported.

The intended use is probability calibration, where the single feature is a
classifier's score. Monotonicity is what makes that safe: a calibration map
that reorders scores changes the ranking and so changes AUC, and a
non-decreasing map cannot. Pass it to \link{calibrate} to use it instead of the
default calibrator, \link{setup_Isotonic}.

Relative to \code{Isotonic}, which is the other monotonic calibrator, this fits on
the logit scale, so it does not saturate at 0 and 1 the way the boundary
bins of isotonic regression do -- it can still round to an endpoint in
double precision
when the input scores are themselves extreme. At \code{smoothness_orders = 1} it
is strictly increasing rather than a step function, so it introduces no ties
and leaves AUC unchanged.

\code{smoothness_orders} is the one substantive choice. 0 fits indicator basis
functions and recovers a step function; 1 fits piecewise linear splines and
yields a continuous map.

\code{penalized = FALSE} removes the lasso penalty, giving the non-parametric
maximum likelihood estimate over the monotonic class. Combined with
\code{smoothness_orders = 0} that is isotonic regression, up to the logit-scale
parameterization.

\code{lambda} is selected by cross-validation inside the fit and is not a search
dimension. \code{seed} fixes that cross-validation's fold assignment; \code{nfolds}
and \code{use_min} control it. Calibration sets are often small, so \code{nfolds} is
an upper bound: training backs off to as many folds as the data supports at
three cases each, and says so at \code{verbosity >= 1}.

\code{get_varimp()} reports the same two measures as \code{setup_HAL}, \code{importance}
and \code{max_coefficient}. Both are of limited use at a single feature.
}
\examples{
monotonichal_hyperparams <- setup_MonotonicHAL(smoothness_orders = 0L)
monotonichal_hyperparams
}
\author{
EDG
}
