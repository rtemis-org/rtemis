% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/030_init.R
\name{tuning_grid}
\alias{tuning_grid}
\title{Tuning grid of a Hyperparameters object}
\usage{
tuning_grid(x, ...)
}
\arguments{
\item{x}{\code{Hyperparameters} object.}
}
\value{
data.frame with one row per combination and one column per searched
hyperparameter, or NULL if nothing needs tuning.
}
\description{
The set of hyperparameter combinations a grid search would fit, one per row.
Derived from the object, so it can be inspected before \code{train()} is called.
}
\details{
The grid is the cross product of the search values, reduced by two rules:
\itemize{
\item A hyperparameter that declares it applies only under certain values of
another is set to \code{NA} -- meaning "left unset for this fit" -- in the rows
that do not meet them. \code{reduce_basis} applies only at \code{smoothness_orders}
of 0, so a search over both drops it from the higher-order rows.
\item Rows that become identical once that is done are collapsed, so a
combination is never fit, scored, and ranked twice.
}

\code{tune_GridSearch()} fits exactly these rows, so what this prints is what will
run.
}
\examples{
# reduce_basis applies only at smoothness order 0, so the six combinations
# of the cross product reduce to four.
tuning_grid(
  setup_HAL(smoothness_orders = c(0L, 1L, 2L), reduce_basis = c(0.1, 0.5))
)
}
\author{
EDG
}
