% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_LINADForest}
\alias{setup_LINADForest}
\title{Setup LINADForest Hyperparameters}
\usage{
setup_LINADForest(
  n_trees = 50L,
  mtry_split = NULL,
  mtry_tree = NULL,
  max_leaves = 20L,
  min_cases_split = 2L,
  min_cases_leaf = 1L,
  min_cases_node_model = NULL,
  nvmax = NULL,
  forward_stop = NULL,
  lambda = NULL,
  alpha = NULL,
  learning_rate = 1,
  root_nvmax = NULL,
  root_lambda = NULL,
  root_alpha = NULL,
  root_learning_rate = 1,
  n_cuts = NULL,
  gamma = 0.1,
  split_binning = NULL,
  split_bin_type = "frequency",
  line_search = "expansion",
  constant_rule = "closed_form",
  node_selection = "local",
  ifw = FALSE,
  force_max_leaves = FALSE,
  smooth_validation_curve = NULL,
  node_model = "forward",
  root_model = NULL,
  split_search = "stump",
  line_search_max = 1000
)
}
\arguments{
\item{n_trees}{(Tunable) Integer [1, Inf): Number of trees in the forest.}

\item{mtry_split}{(Tunable) Optional Integer [1, Inf): Features sampled at each split search. NULL scans every feature the tree holds.}

\item{mtry_tree}{(Tunable) Optional Integer [1, Inf): Features available to a whole tree. NULL gives every tree every feature.}

\item{max_leaves}{(Tunable) Integer [1, Inf): Largest number of terminal nodes to grow in each tree.}

\item{min_cases_split}{(Tunable) Integer [2, Inf): Fewest cases a node may hold and still be considered for splitting.}

\item{min_cases_leaf}{(Tunable) Integer [1, Inf): Fewest cases a split must leave on each side.}

\item{min_cases_node_model}{(Tunable) Optional Integer [1, Inf): Fewest cases needed to fit a linear model at a node. Applies only when \code{node_model} fits one.}

\item{nvmax}{(Tunable) Optional Integer [1, Inf): Most terms forward selection may add beside the intercept. Applies only when \code{node_model} is "forward".}

\item{forward_stop}{(Tunable) Optional Character \{"bic", "aic", "none"\}: Cost a term must earn to be added by forward selection. Applies only when \code{node_model} is "forward".}

\item{lambda}{(Tunable) Optional Numeric [0, Inf): L2 penalty on the node models. Applies only when \code{node_model} is "forward", "ridge" or "elasticnet".}

\item{alpha}{(Tunable) Optional Numeric [0, 1]: Elastic-net mixing, 0 ridge to 1 lasso. Applies only when \code{node_model} is "elasticnet".}

\item{learning_rate}{(Tunable) Numeric (0, 1]: Shrinkage applied to every functional update.}

\item{root_nvmax}{(Tunable) Optional Integer [1, Inf): \code{nvmax} for the root model. NULL uses \code{nvmax}.}

\item{root_lambda}{(Tunable) Optional Numeric [0, Inf): \code{lambda} for the root model. NULL uses \code{lambda}.}

\item{root_alpha}{(Tunable) Optional Numeric [0, 1]: \code{alpha} for the root model. NULL uses \code{alpha}.}

\item{root_learning_rate}{(Tunable) Numeric [0, 1]: Shrinkage applied to the root model's slopes.}

\item{n_cuts}{(Tunable) Optional Integer [2, Inf): Cut points tried per feature. Applies only when \code{split_search} is "exhaustive".}

\item{gamma}{(Tunable) Numeric [0, 1]: Weight a case retains in the branch it does not belong to. 0 is a hard partition.}

\item{split_binning}{(Tunable) Optional Integer [2, Inf): Discretize each numeric feature into this many bins and consider only bin boundaries as splits.}

\item{split_bin_type}{(Tunable) Character \{"frequency", "width"\}: How bin edges are placed.}

\item{line_search}{(Tunable) Character \{"expansion", "child", "none"\}: Scope of the Newton step for each update.}

\item{constant_rule}{(Tunable) Character \{"closed_form", "least_squares"\}: How a node's constant is computed.}

\item{node_selection}{(Tunable) Character \{"local", "global"\}: Criterion for choosing which frontier node to split next.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}

\item{force_max_leaves}{Logical: If TRUE, grow every tree to \code{max_leaves} instead of selecting a size on its out-of-bag cases.}

\item{smooth_validation_curve}{Optional Logical: If TRUE, smooth the out-of-bag curve before reading its minimum. Applies only when \code{force_max_leaves} is FALSE.}

\item{node_model}{Character \{"forward", "ridge", "elasticnet", "constant"\}: Model fitted at each node.}

\item{root_model}{Optional Character \{"forward", "ridge", "elasticnet", "constant"\}: Model fitted at each tree's root. NULL uses \code{node_model}.}

\item{split_search}{Character \{"stump", "exhaustive"\}: How a split is chosen.}

\item{line_search_max}{Numeric (0, Inf): Largest absolute step the line search may take.}
}
\value{
LINADForestHyperparameters object.
}
\description{
Setup hyperparameters for LINADForest: a bagged ensemble of Linear Additive
Trees. Each tree is fitted on a bootstrap sample of the cases, restricted to
\code{mtry_tree} of the features, searching \code{mtry_split} of them at each split,
and chooses its own number of leaves on the cases its bootstrap left out.
}
\details{
LINADForest is designed to be used \strong{untuned}. Every tree-level
hyperparameter of \link{setup_LINAD} is available here and tunable, but the
defaults are the configuration the ensemble was first evaluated with, and
the one structural choice that usually needs tuning -- how large each tree
should be -- is made per tree from data.
}
\section{How the trees differ}{

Three sources of variation, each acting at a different level:

\describe{
\item{The bootstrap}{Each tree is fitted on \code{n} cases drawn with
replacement, so roughly 37\% of cases are out-of-bag for any given tree.}
\item{\code{mtry_tree}}{Each tree is restricted to a random subset of the
features, for its splits and its node models alike. A tree's coefficients
then cover only the features it held.}
\item{\code{mtry_split}}{Each split search sees a fresh sample of the features
the tree holds, which decorrelates the trees further. If that sample
finds no split worth making, the node is searched again over every
feature, so \code{mtry_split} changes which split is made and never whether a
node can split at all.}
}

Both \verb{mtry_*} default to NULL, meaning every feature, which is the
configuration the ensemble was first evaluated with.
}

\section{Choosing each tree's size}{

A LINAD tree picks its number of leaves on held-out data, as gradient
boosting picks its number of trees. A bootstrap produces held-out data for
free, so each tree selects its own size on its own out-of-bag cases and no
validation set has to be supplied or set aside. \code{force_max_leaves = TRUE}
turns the selection off and grows every tree to \code{max_leaves}, which is what
the ensemble's first evaluation did.

The same out-of-bag predictions give the fitted forest an error estimate with
no resampling, reported by \code{print()}.
}

\section{Defaults that differ from a single tree}{

\code{learning_rate} is 1 rather than 0.1. A lone LINAD tree shrinks each node
update because nothing else controls its variance; in a forest the averaging
does that, so each tree should be a strong learner -- the same reasoning that
leaves a random forest's trees unpruned.
}

\section{Workers}{

LINADForest parallelizes across trees, by process rather than by thread.
\code{n_workers_algorithm} therefore has an effect only when the run is not
already dispatching: inside a parallel outer fold the forest runs its trees
sequentially, since a fold may not dispatch again. For a threaded backend
such as Ranger the same setting means threads within each fold.
}

\examples{
linadforest_hyperparams <- setup_LINADForest(n_trees = 25L)
linadforest_hyperparams
}
\references{
Wager S, Hastie T, Efron B (2014).
Confidence intervals for random forests: the jackknife and the infinitesimal
jackknife. \emph{Journal of Machine Learning Research}, 15, 1625-1651.
}
\author{
EDG
}
