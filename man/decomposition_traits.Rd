% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algorithmDB.R
\name{decomposition_traits}
\alias{decomposition_traits}
\title{Decomposition Algorithm Traits}
\usage{
decomposition_traits(algorithm = NULL)
}
\arguments{
\item{algorithm}{Optional Character: Name of a decomposition algorithm,
matched case-insensitively. \code{NULL} returns every algorithm.}
}
\value{
data.frame: One row per algorithm, one column per trait.
}
\description{
Machine-readable properties of each decomposition algorithm available to
\link{decomp}: whether the map is linear, whether it can be applied to new data,
whether it inverts, and what its objective preserves.
}
\details{
The columns:

\describe{
\item{\code{name}}{The name used by \verb{setup_<name>()} and \code{decomp(algorithm =)}.}
\item{\code{description}}{Full name of the algorithm.}
\item{\code{linear}}{The map from input space to component space is linear.}
\item{\code{can_apply}}{A fitted result can be applied to new data with
\link{apply_decomp}.}
\item{\code{invertible}}{There is an inverse map from component space back to
input space, so reconstruction error is defined.}
\item{\code{orthogonal}}{Components are mutually orthogonal.}
\item{\code{ordered}}{Components come in a meaningful order, so "the first j"
is well-defined.}
\item{\code{deterministic}}{The same input and configuration give the same
result without setting a seed.}
\item{\code{preserves}}{What the objective preserves: \code{"variance"},
\code{"global"}, \code{"local"}, or \code{"reconstruction"}.}
\item{\code{nonneg}}{Requires non-negative input and produces non-negative
factors.}
\item{\code{package}}{Package supplying the backend implementation.}
}

\code{can_apply} states what rtemis implements, not what is mathematically
possible. Parametric tSNE exists and Isomap admits a Nystrom-style
out-of-sample extension; neither is implemented here, so both are \code{FALSE}.
The column's contract is that \link{apply_decomp} on a fitted result of this
algorithm returns components rather than an error.
}
\examples{
decomposition_traits()
decomposition_traits("PCA")
# Which algorithms support out-of-sample projection?
decomposition_traits()[["name"]][decomposition_traits()[["can_apply"]]]
}
\author{
EDG
}
