% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/120_ClusteringConfig.R
\name{setup_SpectralRBF}
\alias{setup_SpectralRBF}
\title{Setup SpectralRBFConfig}
\usage{
setup_SpectralRBF(
  k = 3L,
  sigma = NULL,
  sigma_sample_fraction = NULL,
  iterations = 200L,
  nystrom = NULL,
  features = NULL
)
}
\arguments{
\item{k}{Integer [2, Inf): Number of clusters. Must be no more than the number of cases.}

\item{sigma}{Optional Numeric (0, Inf): Inverse kernel width. Unset estimates it from the data.}

\item{sigma_sample_fraction}{Optional Numeric (0, 1]: Fraction of the cases the kernel width is estimated from when \code{sigma} is unset. Unset uses three quarters of them.}

\item{iterations}{Integer [1, Inf): Maximum number of k-means iterations run on the spectral embedding.}

\item{nystrom}{Optional \code{NystromConfig}: Approximate the affinity matrix from a sample of the cases, from \link{setup_Nystrom}. \code{NULL} decomposes it in full.}

\item{features}{Optional Character vector: Names of at least 2 distinct
feature columns to cluster on. \code{NULL} clusters on all numeric features.}
}
\value{
\code{SpectralRBFConfig} object.
}
\description{
Setup a \code{SpectralRBFConfig} object for spectral clustering with a Gaussian
(radial basis function) kernel, via the 'kernlab' package.
}
\details{
Spectral clustering builds a similarity graph over the cases, embeds them
in the leading eigenvectors of that graph's normalized Laplacian, and runs
k-means there. Groups that are connected but not compact -- concentric
rings, elongated bands -- separate in that embedding while k-means on the
raw features cannot find them. This variant measures similarity with a
Gaussian kernel whose width is shared by every case; see
\link{setup_SpectralLocal} for a per-case width and \link{setup_SpectralLaplace} for
an exponential kernel.

The cost is cubic in the number of cases, since the whole affinity matrix
is decomposed. Pass \code{nystrom} to approximate the decomposition from a
sample instead.

Argument names are rtemis' own. They map to \code{kernlab::specc()} as: \code{k} ->
\code{centers}, \code{sigma} -> \code{kpar = list(sigma)} with \code{kernel = "rbfdot"},
\code{sigma_sample_fraction} -> \code{mod.sample} (with \code{kpar = "automatic"} when
\code{sigma} is unset), \code{nystrom} -> \code{nystrom.red} and \code{nystrom.sample};
\code{iterations} keeps its name.
}
\examples{
spectral_config <- setup_SpectralRBF(k = 3L)
spectral_config
setup_SpectralRBF(k = 3L, nystrom = setup_Nystrom(sample = 100L))
}
\references{
Ng AY, Jordan MI, Weiss Y (2001). On Spectral Clustering: Analysis and an
Algorithm. \emph{Advances in Neural Information Processing Systems}, 14,
849-856.
}
\author{
EDG
}
