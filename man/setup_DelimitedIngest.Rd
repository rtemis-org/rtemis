% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/047_IngestConfig.R
\name{setup_DelimitedIngest}
\alias{setup_DelimitedIngest}
\title{Set up a \code{DelimitedIngestConfig}}
\usage{
setup_DelimitedIngest(
  columns = NULL,
  character2factor = TRUE,
  clean_colnames = TRUE,
  remove_duplicates = FALSE,
  reader = "data.table",
  sep = NULL,
  quote = "\\"",
  na_strings = ""
)
}
\arguments{
\item{columns}{Optional Named vector: Declared column types, keyed by column
name, in the \code{profile/v1} vocabulary -- \code{c(Age = "integer")}. A column named
here is converted after reading rather than inferred, which is what makes a
known type beat a heuristic.}

\item{character2factor}{Logical: If TRUE, read character columns as factors.}

\item{clean_colnames}{Logical: If TRUE, normalize column names.}

\item{remove_duplicates}{Logical: If TRUE, drop rows repeating an earlier row.}

\item{reader}{Character \{"data.table", "vroom", "duckdb", "arrow"\}: Library
used to read the file.}

\item{sep}{Optional Character: Field separator. If \code{NULL}, the reader detects it.}

\item{quote}{Character: Quote character.}

\item{na_strings}{Character vector: Strings to read as missing.}
}
\value{
\code{DelimitedIngestConfig} object.
}
\description{
Reading a delimited file -- the format that declares nothing, and therefore
the one with the most to say.

One constructor per format rather than one for all of them: a Parquet has no
separator, so \code{sep} is not a setting it ignores, it is not a setting. R
refusing an argument that does not exist says that better than any check
could, and each format's own enums and defaults can then be documented
against the class they belong to.
}
\examples{
setup_DelimitedIngest(sep = ";")
}
\author{
EDG
}
