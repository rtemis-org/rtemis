% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/100_Resampler.R
\name{setup_StratSub}
\alias{setup_StratSub}
\title{Setup StratSub Resampler}
\usage{
setup_StratSub(
  n_resamples = 10L,
  train_p = 0.75,
  stratify_var = NULL,
  strat_n_bins = 4L,
  id_strat = NULL,
  seed = NULL
)
}
\arguments{
\item{n_resamples}{Integer [1, Inf): Number of resamples to make.}

\item{train_p}{Numeric (0, 1): Training set percentage.}

\item{stratify_var}{Optional Character: Variable to stratify by.}

\item{strat_n_bins}{Integer [1, Inf): Number of bins to stratify by.}

\item{id_strat}{Optional Character: Name of the column holding per-case grouping IDs, e.g.
subject IDs when the dataset contains repeated measurements. Cases sharing an ID stay in the
same resample, so a case can only be present in the training or the test set, not both.
Requires that \code{resample()} be given the data frame the column lives in. \link{train} takes the
column to identify cases rather than describe them, so it groups the resamples by it and
excludes it from the features.}

\item{seed}{Optional Integer [0, Inf): Random seed.}
}
\value{
\code{StratSubConfig} object.
}
\description{
Setup StratSub Resampler
}
\examples{
strat_sub_resampler <- setup_StratSub(n_resamples = 10L, seed = 2026L)
strat_sub_resampler
}
\author{
EDG
}
