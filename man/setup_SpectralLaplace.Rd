% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/120_ClusteringConfig.R
\name{setup_SpectralLaplace}
\alias{setup_SpectralLaplace}
\title{Setup SpectralLaplaceConfig}
\usage{
setup_SpectralLaplace(
  k = 3L,
  sigma = NULL,
  iterations = 200L,
  nystrom = NULL,
  features = NULL
)
}
\arguments{
\item{k}{Integer [2, Inf): Number of clusters. Must be no more than the number of cases.}

\item{sigma}{Optional Numeric (0, Inf): Inverse kernel width. Unset uses 1.}

\item{iterations}{Integer [1, Inf): Maximum number of k-means iterations run on the spectral embedding.}

\item{nystrom}{Optional \code{NystromConfig}: Approximate the affinity matrix from a sample of the cases, from \link{setup_Nystrom}. \code{NULL} decomposes it in full.}

\item{features}{Optional Character vector: Names of at least 2 distinct
feature columns to cluster on. \code{NULL} clusters on all numeric features.}
}
\value{
\code{SpectralLaplaceConfig} object.
}
\description{
Setup a \code{SpectralLaplaceConfig} object for spectral clustering with an
exponential (Laplacian) kernel, via the 'kernlab' package. The exponential
kernel decays more slowly than the Gaussian of \link{setup_SpectralRBF} and so
keeps more weight on distant pairs.
}
\details{
Argument names are rtemis' own. They map to \code{kernlab::specc()} as: \code{k} ->
\code{centers}, \code{sigma} -> \code{kpar = list(sigma)} with \code{kernel = "laplacedot"},
\code{nystrom} -> \code{nystrom.red} and \code{nystrom.sample}; \code{iterations} keeps its
name.
}
\examples{
setup_SpectralLaplace(k = 3L, sigma = 0.5)
}
\references{
Ng AY, Jordan MI, Weiss Y (2001). On Spectral Clustering: Analysis and an
Algorithm. \emph{Advances in Neural Information Processing Systems}, 14,
849-856.
}
\author{
EDG
}
