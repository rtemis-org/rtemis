% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_config.R
\name{write_record}
\alias{write_record}
\title{Write a run record to a JSON file}
\usage{
write_record(x, file, overwrite = FALSE, verbosity = 1L)
}
\arguments{
\item{x}{Fitted model object, or a record list from \link{record}.}

\item{file}{Character: Path to write to.}

\item{overwrite}{Logical: If TRUE, overwrite an existing file.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{x}, invisibly.
}
\description{
A record states what a run actually did: every value resolved, where each one
came from, and what produced it. Deliberately a separate function rather than
an argument to \link{write_config} -- the two artifacts answer different
questions, and a caller should have to say which it wants.
}
\details{
Three things distinguish a record from the config it came from:
\itemize{
\item \strong{Every field is present and resolved.} A config omits what the user did
not set, so a reader applies defaults; a record leaves nothing to them, and
writes an unset field as an explicit \code{null}.
\item \strong{Each value says where it came from}, in a parallel \code{origin} map:
\code{user}, \code{default}, \code{derived} (computed from the data), \code{tuned}, or \code{unset}
for a field a failed run never reached.
\item \strong{\code{provenance}} records the rtemis and R versions, platform, timing,
outcome, and a fingerprint of the data.
}

For a supervised run the top level is what was \emph{asked for} and \code{folds} is
what \emph{ran}, one entry per model fitted -- outer resampling resolves different
values in each fold, so a single resolved value at the top level would state
something no fold did.

A supervised record also states \strong{what the run scored}. \code{metrics} holds each
sample's headline row as a flat metric-to-value map, averaged across outer
resamples, with \code{metrics_sd} beside it for the spread (\code{null} for a single
fit, which has none). The full metrics -- the confusion matrix, the per-class
rows -- are in each fold's own \code{metrics}. The flat block exists so that "was
this model any good?" is one lookup in one file, with no averaging and no R,
which is what makes a directory of records rankable.

\link{train}, \link{decomp} and \link{cluster} call this automatically when given an
\code{outdir}. A record is not a config: feeding one to \link{read_config} is an error,
since its resolved values would silently pin settings the new call should
decide for itself.

Unlike a config, a record is \strong{not compacted}: an unset field is written as
an explicit \code{null} rather than omitted, so nothing in it falls back to a
reader's defaults. That is the whole claim a record makes.
}
\examples{
\dontrun{
mod <- train(iris, hyperparameters = setup_CART())
write_record(mod, "train_CART.record.json")
}
}
\author{
EDG
}
