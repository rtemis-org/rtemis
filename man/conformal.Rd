% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/030_init.R
\name{conformal}
\alias{conformal}
\title{Conformal prediction regions}
\usage{
conformal(x, newdata, calibration = NULL, config = NULL, verbosity = 1L, ...)
}
\arguments{
\item{x}{\code{Supervised} or \code{SupervisedRes} object.}

\item{newdata}{tabular data: Cases to bound. Predictors only, in training
order, as \code{\link[stats:predict]{stats::predict()}} requires.}

\item{calibration}{Optional tabular data: Calibration cases, predictors and
outcome, in the shape \code{train()} was given. NULL uses the model's stored test
split where it has one.}

\item{config}{Optional \code{ConformalConfig} object: Built by
\link{setup_SplitConformal}, \link{setup_CVPlus} or \link{setup_CQR}. NULL takes the method
the object supports.}

\item{verbosity}{Integer: Verbosity level.}

\item{...}{Additional arguments passed to methods.}
}
\value{
\code{PredictionInterval} object for a regression, \code{PredictionSet} for a
classification.
}
\description{
A prediction interval for a regression or a set of labels for a
classification, covering the truth with probability at least \code{1 - alpha}
under exchangeability alone -- in finite samples, for any model, including a
misspecified one.
}
\details{
\strong{The guarantee is marginal.} \verb{P(Y in C(X)) >= 1 - alpha} averages over the
draw of both the calibration set and the test case. It is \emph{not} conditional:
90\% marginal coverage is compatible with 99\% coverage on easy cases and 60\%
on a hard subgroup, and averaged over everyone is not the same as guaranteed
for anyone. It is also void, silently, under distribution shift --
exchangeability is the whole assumption.

\strong{Width is the quality measure; coverage is the correctness check.} A
useless model attains valid coverage by returning intervals wide enough to be
uninformative, so read the two together. The region reports its widths (or
set sizes); \code{conformal_metrics()} scores coverage against outcomes the region
did not see.

\strong{Which data calibrates.} \code{calibration} is a tabular dataset holding the
predictors and the outcome, in training shape. Left NULL, a model trained
with \code{dat_test} calibrates on that split, whose residuals it already stores:
\code{train()} never fits, tunes or early-stops on \code{dat_test}. Nothing else is
assumed clean -- a validation split may have driven early stopping, and a
model chosen by its test metric has used that split for selection, which
nothing in the object records.

Conformal calibration is unrelated to the probability calibration
\link{calibrate} performs, and the two must not share rows. Where rtemis can see
that they do, it refuses.

\strong{Which method.} \code{config} NULL takes the method the object supports: split
conformal for a \code{Supervised}, CV+ for a \code{SupervisedRes}. See
\link{setup_SplitConformal}, \link{setup_CVPlus} and \link{setup_CQR}.
}
\examples{
x <- data.frame(age = rnorm(300), bmi = rnorm(300))
x[["y"]] <- x[["age"]] * 2 + rnorm(300, sd = 0.3)
# Three splits: one fits, one calibrates, one scores what the other two did.
mod <- train(
  x[1:200, ],
  dat_test = x[201:250, ],
  hyperparameters = setup_GLM(),
  verbosity = 0L
)

# The stored test split calibrates; 90\% intervals for five fresh cases.
region <- conformal(mod, x[1:5, c("age", "bmi")], verbosity = 0L)
region
region@lower
region@upper

# Coverage, against outcomes nothing in the pipeline has seen.
held_out <- x[251:300, ]
conformal_metrics(
  conformal(mod, held_out[, c("age", "bmi")], verbosity = 0L),
  held_out[["y"]]
)
}
\author{
EDG
}
