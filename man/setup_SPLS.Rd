% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_SPLS}
\alias{setup_SPLS}
\title{Setup SPLS Hyperparameters}
\usage{
setup_SPLS(
  k = 2L,
  eta = 0.5,
  kappa = 0.5,
  select = "pls2",
  fit = "simpls",
  classifier = "lda",
  scale_x = TRUE,
  scale_y = FALSE,
  eps = 1e-04,
  maxstep = 100L,
  ifw = FALSE
)
}
\arguments{
\item{k}{(Tunable) Integer [1, Inf): Number of latent components.}

\item{eta}{(Tunable) Numeric [0, 1): Sparsity threshold. Higher values select fewer features.}

\item{kappa}{(Tunable) Numeric [0, 0.5]: Concavity of the surrogate direction vector problem.}

\item{select}{Character \{"pls2", "simpls"\}: Feature selection algorithm (regression only).}

\item{fit}{Character \{"kernelpls", "widekernelpls", "simpls", "oscorespls"\}: PLS algorithm used for model fitting (regression only).}

\item{classifier}{Character \{"lda", "logistic"\}: Classifier fit on the latent components (classification only).}

\item{scale_x}{Logical: If TRUE, scale features to unit variance.}

\item{scale_y}{Logical: If TRUE, scale the outcome to unit variance (regression only).}

\item{eps}{Numeric (0, Inf): Convergence tolerance (regression only).}

\item{maxstep}{Integer [1, Inf): Maximum number of iterations per component (regression only).}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
SPLSHyperparameters object.
}
\description{
Setup hyperparameters for Sparse Partial Least Squares training.
}
\details{
Regression is fit with \link[spls:spls]{spls::spls} and classification with
\link[spls:splsda]{spls::splsda}, chosen from the outcome type. Parameters marked
"regression only" or "classification only" are passed to the backend that
accepts them and ignored by the other.

\code{spls} provides no case weights, so \code{ifw} cannot be honored: enabling it
makes training abort rather than silently fit an unweighted model.
}
\examples{
spls_hyperparams <- setup_SPLS(k = 3L, eta = 0.7)
spls_hyperparams
}
\author{
EDG
}
