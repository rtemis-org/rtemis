% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/260_PredictionRegion.R
\name{setup_CVPlus}
\alias{setup_CVPlus}
\title{Setup CV+ Conformal Prediction}
\usage{
setup_CVPlus(alpha = 0.1, score = NULL, seed = NULL)
}
\arguments{
\item{alpha}{Numeric (0, 1): Miscoverage rate. The region covers with
probability at least \code{1 - 2 * alpha} in the worst case.}

\item{score}{Optional Character \{"absolute", "LAC", "APS"\}: Nonconformity
score. NULL resolves to "absolute" for a regression and "APS" for a
classification. See \link{setup_SplitConformal} for the trade between the two
set-valued scores, and for why APS draws.}

\item{seed}{Optional Integer [0, Inf): Random seed for the APS draw. NULL
draws one and records it.}
}
\value{
\code{CVPlusConfig} object.
}
\description{
Configure CV+, jackknife+ and cross-conformal prediction for \link{conformal}
over a model trained with outer resampling.
}
\details{
CV+ (Barber, Candes, Ramdas and Tibshirani, 2021) spends no data on
calibration: every case is out-of-fold exactly once, so every case both
trains and calibrates. Over a \code{SupervisedRes} the expensive part is already
paid -- the per-fold models and their out-of-fold predictions are stored --
and what remains is predicting \code{newdata} under each fold's model and taking
two order statistics.

\strong{It delivers \code{1 - 2 * alpha} in the worst case}, not \code{1 - alpha}. The bound
is conservative and observed coverage usually sits near \code{1 - alpha}, but the
region states the guarantee it carries rather than letting a reader assume
the tighter one.

\strong{The folds must partition the cases}, each case out-of-fold exactly once.
\code{setup_KFold()} and \code{setup_LOOCV()} do; the subsampling and
bootstrap types do not, and \code{conformal()} refuses them by checking the
indices rather than the type name.

Which relative runs follows from the object: leave-one-out folds make it
jackknife+, and a classification outcome makes it cross-conformal (Vovk,
2015), CV+ being defined for intervals. The result records which.
}
\examples{
setup_CVPlus()
setup_CVPlus(alpha = 0.2)
}
\author{
EDG
}
