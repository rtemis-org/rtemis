% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_DecomposeConfig.R
\name{setup_DecomposeConfig}
\alias{setup_DecomposeConfig}
\title{Setup DecomposeConfig}
\usage{
setup_DecomposeConfig(
  dat_path = NULL,
  algorithm = NULL,
  decomposition_config = NULL,
  outdir = "results/",
  verbosity = 1L
)
}
\arguments{
\item{dat_path}{Character or NULL: Path to input data file. NULL leaves the
recipe unbound; set it (or supply data) before \link{decomp}.}

\item{algorithm}{Character or NULL: Decomposition algorithm. May be left NULL
if \code{decomposition_config} is supplied (it carries its own algorithm).}

\item{decomposition_config}{\code{DecompositionConfig} object or NULL: Configuration
for the decomposition itself. Setup with a decomposition \verb{setup_*} function,
e.g. \link{setup_PCA}. If NULL, defaults for \code{algorithm} are used at \link{decomp} time.}

\item{outdir}{Character: Output directory for results.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{DecomposeConfig} object.
}
\description{
Setup a \code{DecomposeConfig} object. \code{DecomposeConfig} is a portable,
data-agnostic recipe for a decomposition pipeline: \code{dat_path} is optional, so
the same config can be validated, shared, or described without data and have a
path bound later (e.g. by the \code{rtemis} CLI) before \link{decomp}.
}
\examples{
dc <- setup_DecomposeConfig(
  dat_path = "data.csv",
  decomposition_config = setup_PCA(k = 3L),
  outdir = "results/"
)
}
\author{
EDG
}
