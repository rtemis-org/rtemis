% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/120_ClusteringConfig.R
\name{setup_SpectralLocal}
\alias{setup_SpectralLocal}
\title{Setup SpectralLocalConfig}
\usage{
setup_SpectralLocal(k = 3L, iterations = 200L, features = NULL)
}
\arguments{
\item{k}{Integer [2, Inf): Number of clusters. Must be no more than the number of cases.}

\item{iterations}{Integer [1, Inf): Maximum number of k-means iterations run on the spectral embedding.}

\item{features}{Optional Character vector: Names of at least 2 distinct
feature columns to cluster on. \code{NULL} clusters on all numeric features.}
}
\value{
\code{SpectralLocalConfig} object.
}
\description{
Setup a \code{SpectralLocalConfig} object for spectral clustering with a locally
scaled Gaussian kernel, via the 'kernlab' package. Each case's kernel width
is taken from the distance to its seventh nearest neighbor, which lets one
clustering hold groups of differing density. The per-case widths need every
pairwise distance, so this variant has no Nystrom approximation.
}
\details{
Argument names are rtemis' own. They map to \code{kernlab::specc()} as: \code{k} ->
\code{centers}, with \code{kpar = "local"}; \code{iterations} keeps its name.
}
\examples{
setup_SpectralLocal(k = 3L)
}
\references{
Zelnik-Manor L, Perona P (2004). Self-Tuning Spectral Clustering.
\emph{Advances in Neural Information Processing Systems}, 17, 1601-1608.
}
\author{
EDG
}
