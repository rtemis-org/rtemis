% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/050_ExecutionConfig.R
\name{setup_SerialExecution}
\alias{setup_SerialExecution}
\title{Set up a \code{SerialExecutionConfig}}
\usage{
setup_SerialExecution(
  n_workers_outer = NULL,
  n_workers_tuning = NULL,
  n_workers_algorithm = NULL,
  on_error = c("continue", "stop", "stop_outer"),
  seed = NULL,
  shared_memory = c("auto", "none", "always"),
  warm_workers = TRUE
)
}
\arguments{
\item{n_workers_outer}{Optional Integer [1, 1]: Workers for outer resampling.
Serial execution dispatches nothing, so 1 or unset.}

\item{n_workers_tuning}{Optional Integer [1, 1]: Workers for tuning. Serial
execution dispatches nothing, so 1 or unset.}

\item{n_workers_algorithm}{Optional Integer [1, Inf): Threads for a self-parallelizing
algorithm, overriding the automatic assignment.}

\item{on_error}{Character \{"continue", "stop", "stop_outer"\}: Failure policy.
\code{"continue"} makes grid cells and unscorable hyperparameter combinations
non-fatal (recorded, warned, and excluded), failing only when nothing is scorable or
the final model fails; \code{"stop"} aborts on any error; \code{"stop_outer"} tolerates grid-cell
failures but aborts on an outer-fold failure.}

\item{seed}{Optional Integer [0, Inf): Master seed for the run's computation RNG,
from which one independent substream per parallel task is derived. Left \code{NULL}, a
seed is drawn from the current RNG stream and recorded on the returned object, so
every run is reproducible and the seed it used is auditable. Distinct from a
\code{ResamplerConfig} seed, which governs how the data is split.}

\item{shared_memory}{Character \{"none", "auto", "always"\}: Whether to hand workers
the training data through OS shared memory instead of serializing a copy to each.
\code{"auto"} is the default: it shares whenever it can -- workers parallel and on this
machine, \pkg{mori} installed -- and quietly does not when it cannot, falling back to
the ordinary transport. \code{"none"} disables it. \code{"always"} is a demand rather than a
preference: it shares even when the run is sequential, which is what allows a run to
be compared against its own shared counterpart, and raises rather than degrades when
the request cannot be honored -- which is what a caller relying on sharing to stay
inside a memory budget needs.}

\item{warm_workers}{Logical: Load \pkg{rtemis} in every worker as the pool is built,
rather than leaving each worker to load it on its first task.}
}
\value{
\code{SerialExecutionConfig} object.
}
\description{
Execution in the calling process: nothing is dispatched to a worker. Takes
no \code{n_workers} and no \code{future_plan}, neither of which a serial run can act
on.
}
\details{
\code{n_workers_algorithm} still applies: it is threads \emph{within the calling
process}, not worker processes, so a self-parallelizing algorithm runs
multi-threaded under a serial config.

See \link{setup_FutureExecution} for how the run's seed is resolved.
}
\examples{
setup_SerialExecution()
}
\author{
EDG
}
