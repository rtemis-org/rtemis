% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_HAL}
\alias{setup_HAL}
\title{Setup HAL Hyperparameters}
\usage{
setup_HAL(
  max_degree = 2L,
  smoothness_orders = 1L,
  reduce_basis = NULL,
  num_knots = NULL,
  use_min = TRUE,
  nfolds = 10L,
  max_basis = 5000000L,
  seed = NULL,
  ifw = FALSE
)
}
\arguments{
\item{max_degree}{(Tunable) Integer [1, Inf): Highest order of interaction among features that a basis function may represent.}

\item{smoothness_orders}{(Tunable) Integer [0, 9]: Smoothness of the basis functions: 0 fits zero-order indicators, 1 piecewise linear splines, higher values higher-order splines.}

\item{reduce_basis}{(Tunable) Optional Numeric (0, 1]: Minimum proportion of cases a basis function must be non-zero in to be kept. Applies only when \code{smoothness_orders} is 0; a search that also covers higher orders drops it from those grid cells.}

\item{num_knots}{Optional Integer [1, Inf) vector: Number of knots per interaction degree, one value per degree, non-increasing. NULL generates them from max_degree and smoothness_orders.}

\item{use_min}{Logical: If TRUE, select \code{lambda.min} from the internal cross-validation; if FALSE, the more heavily penalized \code{lambda.1se}.}

\item{nfolds}{Integer [3, Inf): Number of folds of the internal cross-validation that selects lambda.}

\item{max_basis}{Integer [1, Inf): Largest projected basis-function count that will be fit.}

\item{seed}{Optional Integer: Random seed for the internal cross-validation's fold assignment. NULL leaves it drawn from the ambient RNG.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
HALHyperparameters object.
}
\description{
Setup hyperparameters for Highly Adaptive Lasso training.
}
\details{
Both outcome types are fit with \link[hal9001:fit_hal]{hal9001::fit_hal}, which spans the outcome
with a basis of indicator or spline terms over every interaction of the
features up to \code{max_degree}, then fits a lasso over that basis.
Regression uses the gaussian family, binary classification the binomial one;
\code{hal9001} has no multinomial family, so multiclass classification is not
supported.

\code{lambda} is selected by cross-validation inside the fit and is not a search
dimension. \code{seed} fixes that cross-validation's fold assignment; \code{nfolds}
and \code{use_min} control it.

\code{hal9001} takes a numeric matrix, so factors are one-hot encoded first and
the encoder is re-applied at predict time.

Scaling: the basis has one function per knot per feature subset, so its size
grows as \code{C(n_features, max_degree)} and, through the knots, with the number
of cases -- making the lasso that follows quadratic in the number of cases.
Training projects the basis size up front, reports it at \code{verbosity >= 1},
warns past a million, and aborts past \code{max_basis}. \code{max_degree} is the
strongest lever on that count, \code{num_knots} the next.

\code{get_varimp()} reports two measures, both aggregated over the basis
functions that involve each feature:
\itemize{
\item \code{importance}: the sum of the absolute values of their non-zero
coefficients.
\item \code{max_coefficient}: the largest single such absolute coefficient,
separating a feature carried by one strong term from one carried by many
weak ones.
}

\code{plot_varimp(mod, measure = "max_coefficient")} plots the second; the first
is the default. Both read coefficients on the scale of the basis functions.
At \code{smoothness_orders = 0} the basis is made of indicators, so the
coefficients are unit-free and directly comparable across features; at
higher orders each basis function carries the units of its feature, so scale
the features first if they are not already comparable.
}
\examples{
hal_hyperparams <- setup_HAL(max_degree = 1L)
hal_hyperparams
}
\author{
EDG
}
