% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/048_PartitionConfig.R
\name{partition}
\alias{partition}
\title{Split a dataset into training and held-out test partitions}
\usage{
partition(dat, config, outdir = NULL, overwrite = FALSE, verbosity = 1L)
}
\arguments{
\item{dat}{data.frame or data.table: The dataset to split.}

\item{config}{\code{PartitionConfig}: The split rule.}

\item{outdir}{Optional Character: Directory to write \code{training.parquet} and
\code{test.parquet} to. If \code{NULL}, nothing is written and the manifest's
\code{output} entries are the in-memory frames themselves.}

\item{overwrite}{Logical: If TRUE, overwrite existing output files.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
Named list: the partition manifest -- \code{config}, \code{source} (a
\code{DataFingerprint} of \code{dat}), and \code{outputs} (a list with \code{training} and
\code{test}, each a \code{DataFingerprint}, plus the path written to when \code{outdir} is
given).
}
\description{
A held-out split is a decision -- which rows train, which are held out, and
by what rule -- and a decision the record cannot report is one nothing
downstream can audit. \code{partition()} makes it a first-class, executed step,
the same way \code{ingest()} makes reading a file one: the config states the
rule, this function applies it and returns a manifest naming exactly what
happened, including a \code{DataFingerprint} of each side.

Not a \code{SuperConfig} property. \code{SuperConfig}'s \code{dat_training_path} and
\code{dat_test_path} simply name whatever \code{partition()} (or any other step)
produced -- the same relationship they already have with \code{ingest()}'s
output.
}
\examples{
partition(iris, setup_RandomPartition(train_p = 0.8, seed = 2026L))
}
\author{
EDG
}
