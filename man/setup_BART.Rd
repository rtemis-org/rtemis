% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_BART}
\alias{setup_BART}
\title{Setup BART Hyperparameters}
\usage{
setup_BART(
  num_trees = 200L,
  alpha = 0.95,
  beta = 2,
  min_samples_leaf = 5L,
  max_depth = 10L,
  num_features_subsample = NULL,
  variance_forest_num_trees = 0L,
  num_gfr = 5L,
  num_burnin = 0L,
  num_mcmc = 100L,
  num_chains = 1L,
  keep_every = 1L,
  cutpoint_grid_size = 100L,
  standardize = TRUE,
  link = "probit",
  seed = NULL,
  ifw = FALSE
)
}
\arguments{
\item{num_trees}{(Tunable) Integer [1, Inf): Number of trees in the mean forest.}

\item{alpha}{(Tunable) Numeric (0, 1): Base of the tree split prior \code{alpha * (1 + depth)^-beta}.}

\item{beta}{(Tunable) Numeric [0, Inf): Depth penalty exponent of the tree split prior \code{alpha * (1 + depth)^-beta}.}

\item{min_samples_leaf}{(Tunable) Integer [1, Inf): Minimum number of training cases in a leaf.}

\item{max_depth}{(Tunable) Optional Integer [1, Inf): Maximum depth of any tree. NULL imposes no limit.}

\item{num_features_subsample}{(Tunable) Optional Integer [1, Inf): Number of features subsampled when growing each tree. NULL uses every feature.}

\item{variance_forest_num_trees}{(Tunable) Integer [0, Inf): Number of trees in the conditional variance forest. 0 fits a homoskedastic model, any larger value a heteroskedastic one.}

\item{num_gfr}{Integer [0, Inf): Number of grow-from-root warm-start iterations.}

\item{num_burnin}{Integer [0, Inf): Number of burn-in MCMC iterations.}

\item{num_mcmc}{Integer [1, Inf): Number of retained MCMC iterations per chain.}

\item{num_chains}{Integer [1, Inf): Number of independent MCMC chains. Cannot exceed \code{num_gfr} unless \code{num_gfr} is 0.}

\item{keep_every}{Integer [1, Inf): Thinning interval: retain one MCMC sample in every \code{keep_every}.}

\item{cutpoint_grid_size}{Integer [1, Inf): Maximum number of candidate cutpoints considered by the grow-from-root algorithm.}

\item{standardize}{Logical: If TRUE, center and scale the outcome before sampling.}

\item{link}{Character \{"probit", "cloglog"\}: Link function of the binary outcome model. Classification only.}

\item{seed}{Optional Integer: Random seed for the sampler. NULL leaves the sampler seeded by the system.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
BARTHyperparameters object.
}
\description{
Setup hyperparameters for Bayesian Additive Regression Trees training.
}
\details{
Both outcome types are fit with \link[stochtree:bart]{stochtree::bart}, which samples a sum-of-trees
model by MCMC, optionally warm-started by the grow-from-root algorithm.
Regression uses a continuous outcome model, binary classification a discrete
one with the link named by \code{link}. Multiclass classification is not supported.

Factors are expanded by the backend, so no encoding is needed beforehand.
Case weights scale the residual variance and are honored under the default
\code{link = "probit"}, but \code{stochtree} rejects them under \code{"cloglog"}, so that
combination makes training abort rather than silently fit an unweighted model.
}
\examples{
bart_hyperparams <- setup_BART(num_trees = 50L, num_mcmc = 200L)
bart_hyperparams
}
\author{
EDG
}
