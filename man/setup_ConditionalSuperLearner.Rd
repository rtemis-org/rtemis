% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/115_MetaLearners.R
\name{setup_ConditionalSuperLearner}
\alias{setup_ConditionalSuperLearner}
\title{Setup ConditionalSuperLearner Hyperparameters}
\usage{
setup_ConditionalSuperLearner(
  base_learners = list(setup_GLM(), setup_GLMNET(), setup_Ranger()),
  meta_learner = setup_Ranger(),
  inner_resampling_config = setup_Resampler(n_resamples = 10L, type = "KFold"),
  n_iterations = 4L,
  loss = NULL,
  init = "full",
  min_region_size = 10L,
  expand_search_spaces = TRUE,
  ifw = FALSE
)
}
\arguments{
\item{base_learners}{List of \code{Hyperparameters} objects: The experts. Names
label the regions and the reported region sizes; unnamed entries are named
after their algorithm.}

\item{meta_learner}{\code{Hyperparameters} object: The oracle, a classifier fitted
on the extended dataset.}

\item{inner_resampling_config}{\code{ResamplerConfig} object: Cross-validation
scheme, which must hold out every case exactly once.}

\item{n_iterations}{(Tunable) Integer [1, Inf): Alternations between fitting
the oracle and refitting the experts.}

\item{loss}{Optional Character \{"squared_error", "log_loss"\}: Per-case loss
the oracle minimizes.}

\item{init}{Character \{"full", "random"\}: Region each expert starts from.}

\item{min_region_size}{Integer [1, Inf): Fewest cases an expert's region may
hold before the expert keeps its previous fit instead of being refitted.}

\item{expand_search_spaces}{Logical: If TRUE, expand a base learner's search
space into one expert per combination.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in
classification.}
}
\value{
ConditionalSuperLearnerHyperparameters object.
}
\description{
Setup hyperparameters for the Conditional SuperLearner, which selects the best
model from a library \emph{conditional on the covariates} rather than combining
them.
}
\details{
The model is \verb{sum_k 1\\\{o(x) = k\\\} F_k(x)}: an "oracle" \code{o} assigns each case to
one of the K "experts" \code{F_k}, and that expert predicts it. Oracle and experts
are fitted by alternating, in the manner of k-means: with the experts fixed,
the oracle minimizes the total loss, which reduces to a weighted K-class
classification over an extended dataset holding each case once per expert; with
the oracle fixed, each expert is refitted on the cases assigned to it. The
losses are cross-validated throughout, so an expert is never scored on a case
it was trained on.

The oracle is a classifier over the original covariates whatever the outcome
is, so with more than two experts it must handle more than two classes. Its
variable importance -- which covariates decide \emph{which model applies} -- is what
\link{get_varimp} returns for a fitted model, and a simple oracle such as
\link{setup_CART} makes the partition itself readable.

Supports regression and binary classification.

Reference: Valdes, Interian, Gennatas & van der Laan, "Conditional Super
Learner", IEEE Transactions on Pattern Analysis and Machine Intelligence
(2022). \doi{10.1109/TPAMI.2021.3131976}
}
\examples{
csl_hyperparams <- setup_ConditionalSuperLearner(
  base_learners = list(setup_GLM(), setup_CART())
)
csl_hyperparams
}
\author{
EDG
}
