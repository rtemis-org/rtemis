% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_config.R
\name{write_config}
\alias{write_config}
\title{Write an rtemis config to a JSON file}
\usage{
write_config(x, file, overwrite = FALSE, verbosity = 1L)
}
\arguments{
\item{x}{A \code{SuperConfig}, \code{DecomposeConfig}, \code{ClusterConfig},
\code{DecompositionConfig}, or \code{ClusteringConfig} object.}

\item{file}{Character: Path to output JSON file.}

\item{overwrite}{Logical: If TRUE, overwrite an existing file.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{x}, invisibly.
}
\description{
Write a config object to a schema.rtemis.org JSON file that can be read back
with \link{read_config} and consumed by rtemislive and the \code{rtemis} CLI. The
emitted JSON carries a \verb{$schema} field identifying the config family, so the
reader can dispatch to the right object:
}
\details{
\itemize{
\item \code{SuperConfig} (from \link{setup_SuperConfig}) -> supervised schema.
\item \code{DecomposeConfig} (from \link{setup_DecomposeConfig}) -> decompose schema.
\item \code{ClusterConfig} (from \link{setup_ClusterConfig}) -> cluster schema.
\item \code{DecompositionConfig} (from \code{setup_PCA()}, \code{setup_ICA()}, ...) ->
decomposition schema.
\item \code{ClusteringConfig} (from \code{setup_KMeans()}, \code{setup_DBSCAN()}, ...) ->
clustering schema.
}
}
\examples{
x <- setup_SuperConfig(
  dat_training_path = "~/Data/iris.csv",
  preprocessor_config = setup_Preprocessor(remove_duplicates = TRUE),
  algorithm = "LightRF",
  hyperparameters = setup_LightRF(),
  outer_resampling_config = setup_Resampler(),
  question = "Can we tell iris species apart given their measurements?",
  outdir = "models/"
)
tmpfile <- file.path(tempdir(), "rtemis.json")
write_config(x, tmpfile, overwrite = TRUE)
}
\author{
EDG
}
