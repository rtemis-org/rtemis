% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/045_DataProfile.R
\name{data_profile}
\alias{data_profile}
\title{Profile a dataset}
\usage{
data_profile(x, n_duplicates = TRUE, fingerprint = FALSE)
}
\arguments{
\item{x}{tabular data: The dataset.}

\item{n_duplicates}{Logical: If TRUE, count rows that repeat an earlier row.
The one whole-table operation here; FALSE leaves the field NULL.}

\item{fingerprint}{Logical: If TRUE, record a \link{data_fingerprint} of \code{x}.
Hashes the whole dataset, so it is off by default.}
}
\value{
\code{DataProfile} object.
}
\description{
Measures the facts \link{validate_config} checks a config against: dimensions,
column types, distinct and missing counts per column, level counts for
low-cardinality categorical columns, and the number of complete cases.
}
\details{
One pass per column and no copy of the data, so profiling is cheap enough to
do per validation rather than caching it.
}
\examples{
p <- data_profile(iris)
p@columns
p@level_counts
}
\author{
EDG
}
