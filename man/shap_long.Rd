% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{shap_long}
\alias{shap_long}
\title{Every contribution, one row per case per feature}
\usage{
shap_long(x, newdata, class = NULL)
}
\arguments{
\item{x}{\code{SHAP} object.}

\item{newdata}{tabular data: The cases \code{x} explains.}

\item{class}{Optional Character: Which class's contributions. NULL takes the
first.}
}
\value{
data.table with columns \code{case}, \code{feature}, \code{value}, \code{numeric_value}
and \code{contribution}. \code{numeric_value} is NA for a feature that is not numeric,
which is what a color scale needs to skip it.
}
\description{
The long form a beeswarm reads: a point per case per feature, positioned by
its contribution and colored by the value the feature took.
}
\details{
Shaped here rather than in a renderer, for the reason \code{shap_case()} gives.
}
\examples{
x <- data.frame(age = rnorm(100), bmi = rnorm(100))
x[["y"]] <- x[["age"]] * 2 + rnorm(100, sd = 0.3)
mod <- train(x, hyperparameters = setup_GLM(), verbosity = 0L)
contributions <- explain(
  mod,
  x[, c("age", "bmi")],
  background = x[, c("age", "bmi")],
  verbosity = 0L
)
head(shap_long(contributions, x[, c("age", "bmi")]))
}
\author{
EDG
}
