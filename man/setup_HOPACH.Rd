% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/120_ClusteringConfig.R
\name{setup_HOPACH}
\alias{setup_HOPACH}
\title{Setup HOPACHConfig}
\usage{
setup_HOPACH(
  dist = "cosangle",
  level_selection = "best",
  max_levels = 15L,
  max_children = 9L,
  max_children_mss = 9L,
  collapse = "seq",
  new_medoid = "medsil",
  mss = "med",
  min_improvement = 0,
  initial_order = "co",
  element_order = "own"
)
}
\arguments{
\item{dist}{Character \{"cosangle", "abscosangle", "euclid", "cor", "abscor"\}: Distance measure.}

\item{level_selection}{Character \{"best", "greedy"\}: Which level of the tree becomes the returned partition. "best": the level minimizing MSS; "greedy": the first level below which MSS increases.}

\item{max_levels}{Integer [1, 15]: Maximum number of levels in the tree.}

\item{max_children}{Integer [2, 9]: Maximum number of children at each node.}

\item{max_children_mss}{Integer [2, 9]: Maximum number of children at each node when computing MSS.}

\item{collapse}{Character \{"seq", "all"\}: How collapsing is performed at each level. "seq": collapse pairs sequentially from the closest while MSS decreases; "all": collapse any pair that decreases MSS.}

\item{new_medoid}{Character \{"medsil", "center", "nn", "uwnn"\}: How the medoid of a collapsed pair is chosen.}

\item{mss}{Character \{"med", "mean"\}: Split silhouette criterion; median or mean.}

\item{min_improvement}{Numeric [0, 1]: Relative improvement in MSS required to accept a collapse step.}

\item{initial_order}{Character \{"co", "clust"\}: How clusters in the first level are ordered.}

\item{element_order}{Character \{"own", "neighbor"\}: How elements are ordered within a cluster.}
}
\value{
\code{HOPACHConfig} object.
}
\description{
Setup a \code{HOPACHConfig} object for Hierarchical Ordered Partitioning and
Collapsing Hybrid (HOPACH) clustering, via the Bioconductor package
'hopach'.
}
\details{
HOPACH identifies the number of clusters itself, by building an ordered
hierarchical tree and selecting the level with maximally homogeneous
clusters under the Median (or Mean) Split Silhouette criterion. There is
therefore no \code{k} to set: \code{max_levels} and \code{max_children} bound the tree, and
the number of clusters found is reported by the resulting \code{Clustering}.

Argument names are rtemis' own. They map to \code{hopach::hopach()} as:
\code{dist} -> \code{d}, \code{level_selection} -> \code{clusters}, \code{max_levels} -> \code{K},
\code{max_children} -> \code{kmax}, \code{max_children_mss} -> \code{khigh},
\code{collapse} -> \code{coll}, \code{new_medoid} -> \code{newmed}, \code{mss} -> \code{mss},
\code{min_improvement} -> \code{impr}, \code{initial_order} -> \code{initord},
\code{element_order} -> \code{ord}.

Two values \code{hopach} documents are not offered, because neither works:
\code{d = "abseuclid"}, which \code{distancematrix()} has no branch for, and
\code{ord = "co"}, which errors for every input.

On some datasets \code{hopach} collapses its tree all the way to a single
cluster and cannot split it further, which ends the run with an error.
Whether it happens depends on the data and on \code{dist}, so another \code{dist}
clusters the same data.
}
\examples{
hopach_config <- setup_HOPACH(dist = "euclid", max_levels = 3L)
hopach_config
}
\references{
van der Laan MJ, Pollard KS (2003). A new algorithm for hybrid hierarchical
clustering with visualization and the bootstrap. \emph{Journal of
Statistical Planning and Inference}, 117(2), 275-303.
\doi{10.1016/S0378-3758(02)00388-9}
}
\author{
EDG
}
