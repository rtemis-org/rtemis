% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{conformal_metrics}
\alias{conformal_metrics}
\title{Score a Conformal Prediction Region}
\usage{
conformal_metrics(region, true_outcome)
}
\arguments{
\item{region}{\code{PredictionRegion} object, from \link{conformal}.}

\item{true_outcome}{Numeric or Factor: The outcomes of the cases \code{region}
bounds, in the same order.}
}
\value{
One-row data.frame. \code{coverage} and \code{n} for either shape, plus
\code{mean_width} and \code{median_width} for an interval, or \code{mean_set_size},
\code{singleton_rate} and \code{empty_rate} for a set.
}
\description{
Empirical coverage and region size for a \code{PredictionRegion}, against the
outcomes of the cases it bounds.
}
\details{
\strong{Coverage on the calibration data is not a check.} The threshold is chosen
as the \code{1 - alpha} empirical quantile of the calibration scores, so coverage
measured back on those same cases is \code{1 - alpha} by construction. Score
against outcomes nothing in the pipeline has seen, or the number means
nothing.

\strong{Read coverage and size together.} A model with no signal attains valid
coverage by returning intervals wide enough to be useless, so coverage alone
cannot separate a good model from a wide one. Width -- or set size, and the
rate of singleton and empty sets -- is the part that carries information
about the model.

Coverage is a proportion over the cases supplied, so it carries the Monte
Carlo error of that sample: 100 cases at a nominal 90\% have a standard error
near 3 points, and a single run landing at 87\% is not evidence of a broken
guarantee.
}
\examples{
x <- data.frame(age = rnorm(300), bmi = rnorm(300))
x[["y"]] <- x[["age"]] * 2 + rnorm(300, sd = 0.3)
mod <- train(
  x[1:200, ],
  dat_test = x[201:250, ],
  hyperparameters = setup_GLM(),
  verbosity = 0L
)
held_out <- x[251:300, ]
region <- conformal(mod, held_out[, c("age", "bmi")], verbosity = 0L)
conformal_metrics(region, held_out[["y"]])
}
\author{
EDG
}
