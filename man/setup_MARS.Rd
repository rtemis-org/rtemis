% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_MARS}
\alias{setup_MARS}
\title{Setup MARS Hyperparameters}
\usage{
setup_MARS(
  degree = 1L,
  penalty = NULL,
  nk = NULL,
  nprune = NULL,
  thresh = 0.001,
  minspan = 0L,
  endspan = 0L,
  newvar_penalty = 0,
  fast_k = 20L,
  pmethod = "backward",
  nfold = 0L,
  ncross = 1L,
  stratify = TRUE,
  fast_beta = 1,
  ifw = FALSE
)
}
\arguments{
\item{degree}{(Tunable) Integer [1, Inf): Maximum degree of interaction. 1 builds an additive model with no interaction terms.}

\item{penalty}{(Tunable) Optional Numeric [-1, Inf): Generalized Cross Validation penalty per knot. NULL uses 3 when \code{degree} is greater than 1 and 2 otherwise.}

\item{nk}{(Tunable) Optional Integer [1, Inf): Maximum number of terms, including the intercept, created by the forward pass. NULL lets earth derive it from the number of features.}

\item{nprune}{(Tunable) Optional Integer [1, Inf): Maximum number of terms, including the intercept, retained after pruning. NULL keeps every term the forward pass created.}

\item{thresh}{(Tunable) Numeric [0, 1): Forward pass stopping threshold: stop once adding a term changes R-squared by less than this.}

\item{minspan}{(Tunable) Integer (-Inf, Inf): Minimum number of observations between knots. 0 derives the value internally, and a negative value instead sets the maximum number of equally spaced knots per feature.}

\item{endspan}{(Tunable) Integer [0, Inf): Minimum number of observations before the first and after the final knot. 0 derives the value internally.}

\item{newvar_penalty}{(Tunable) Numeric [0, Inf): Penalty for adding a feature not already in the model during the forward pass.}

\item{fast_k}{(Tunable) Integer [0, Inf): Maximum number of parent terms considered at each step of the forward pass. 0 disables Fast MARS.}

\item{pmethod}{Character \{"backward", "none", "exhaustive", "forward", "seqrep", "cv"\}: Pruning method. "cv" requires \code{nfold}. Multiclass classification allows only "backward" and "none".}

\item{nfold}{Integer [0, Inf): Number of cross-validation folds used to estimate out-of-fold R-squared. 0 disables cross-validation.}

\item{ncross}{Integer [1, Inf): Number of times the \code{nfold} cross-validation is repeated.}

\item{stratify}{Logical: If TRUE, stratify the cross-validation folds on the outcome.}

\item{fast_beta}{Numeric [0, 1]: Fast MARS aging coefficient.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
MARSHyperparameters object.
}
\description{
Setup hyperparameters for Multivariate Adaptive Regression Splines training.
}
\details{
Get more information from \link[earth:earth]{earth::earth}.

\code{get_varimp()} returns earth's three importance criteria, in this order:
\code{importance} (the GCV criterion), \code{rss} (the RSS criterion), and
\code{subset_proportion} (the fraction of pruning subsets that retain the
feature). See \code{varimp_super} in \code{train_MARS.R} for how each is derived.
}
\examples{
mars_hyperparams <- setup_MARS(degree = 2L, nprune = 10L)
mars_hyperparams
}
\author{
EDG
}
