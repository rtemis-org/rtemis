% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_roc.R
\name{roc_curve}
\alias{roc_curve}
\title{ROC curve coordinates}
\usage{
roc_curve(true_labels, predicted_prob, max_points = NULL)
}
\arguments{
\item{true_labels}{Factor: True outcome labels.}

\item{predicted_prob}{Numeric vector [0, 1]: Predicted probabilities for the
positive class (second level of \code{true_labels}). Or, for multiclass, a
matrix of predicted probabilities with exactly one column per class, in
factor-level order (rtemis's convention); the columns are labelled with
the factor levels regardless of any names on the input.}

\item{max_points}{Optional Integer [2, Inf): Cap on the number of vertices
per curve. \code{NULL} keeps full resolution.}
}
\value{
\code{data.frame} with columns \code{class}, \code{fpr}, \code{tpr}, \code{auc} (the AUC is
constant within a class group, repeated per vertex). Zero rows when no
curve is defined.
}
\description{
Compute the points of one or more ROC curves from true labels and predicted
probabilities, returning tidy \verb{(class, fpr, tpr, auc)} rows rather than a
plot. This is the shared engine behind \link{draw_roc} and is reused anywhere the
curve data is needed without plotly (e.g. shipping the curve to a client).
}
\details{
Binary problems yield a single curve for the positive class (the second
level of \code{true_labels}, rtemis convention); multiclass problems yield one
one-vs-rest curve per class. Points are ordered along the curve; pass
\code{max_points} to down-sample very long curves (one vertex per distinct score)
to a compact, smooth line.
}
\examples{
true_labels <- factor(c("A", "B", "A", "A", "B", "A", "B", "B", "A", "B"))
predicted_prob <- c(0.1, 0.4, 0.35, 0.8, 0.65, 0.2, 0.9, 0.55, 0.3, 0.7)
roc_curve(true_labels, predicted_prob)
}
\author{
EDG
}
