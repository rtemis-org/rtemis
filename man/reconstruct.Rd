% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/170_Decomposition.R
\name{reconstruct}
\alias{reconstruct}
\title{Reconstruct Data from a Decomposition}
\usage{
reconstruct(decom, x, verbosity = 1L)
}
\arguments{
\item{decom}{\code{Decomposition} object.}

\item{x}{Tabular data (data.frame, data.table, or tibble): Data to
reconstruct. Its columns must match those \code{decom} was fitted on.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
A data.frame with the same columns as \code{x}, holding the
reconstruction of the decomposed features.
}
\description{
Encode data with a fitted decomposition and decode it back, giving the
rank-\code{k} approximation of the input in its original units and column layout.
}
\details{
The round trip \link{apply_decomp} leaves half-done. The reconstruction is \code{x}
with everything outside the first \code{k} components removed, i.e. a denoised
\code{x}. The residual, \code{x - reconstruct(decom, x)}, is what the decomposition
could not represent: \code{rowMeans(residual^2)} scores how anomalous each case
is, and \code{colMeans(residual^2)} shows which variables the decomposition fails
to capture.

Available for algorithms whose \code{invertible} trait is TRUE; see
\link{decomposition_traits}. tSNE, UMAP and Isomap embed without an inverse, so
there is no map back for them at all.

The reconstruction is in the units of the data as it was passed to \link{decomp},
with any centering or scaling the backend applied internally undone, so it
can be compared with \code{x} directly. Columns the fit did not decompose (when
\code{decom@config@features} is set) pass through unchanged and column order is
preserved, so the result lines up cell for cell with \code{x}.
}
\examples{
x <- exc(iris, "Species")
iris_pca <- decomp(x, algorithm = "PCA", config = setup_PCA(k = 2L))
reconstructed <- reconstruct(iris_pca, x)
# What the two components could not represent, per case:
head(rowMeans((as.matrix(x) - as.matrix(reconstructed))^2))
}
\author{
EDG
}
