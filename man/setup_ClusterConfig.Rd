% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17_ClusterConfig.R
\name{setup_ClusterConfig}
\alias{setup_ClusterConfig}
\title{Setup ClusterConfig}
\usage{
setup_ClusterConfig(
  dat_path = NULL,
  algorithm = NULL,
  clustering_config = NULL,
  outdir = "results/",
  verbosity = 1L
)
}
\arguments{
\item{dat_path}{Character or NULL: Path to input data file. NULL leaves the
recipe unbound; set it (or supply data) before \link{cluster}.}

\item{algorithm}{Character or NULL: Clustering algorithm. May be left NULL if
\code{clustering_config} is supplied (it carries its own algorithm).}

\item{clustering_config}{\code{ClusteringConfig} object or NULL: Configuration for
the clustering itself. Setup with a clustering \verb{setup_*} function, e.g.
\link{setup_KMeans}. If NULL, defaults for \code{algorithm} are used at \link{cluster} time.}

\item{outdir}{Character: Output directory for results.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{ClusterConfig} object.
}
\description{
Setup a \code{ClusterConfig} object. \code{ClusterConfig} is a portable, data-agnostic
recipe for a clustering pipeline: \code{dat_path} is optional, so the same config
can be validated, shared, or described without data and have a path bound
later (e.g. by the \code{rtemis} CLI) before \link{cluster}.
}
\examples{
cc <- setup_ClusterConfig(
  dat_path = "data.csv",
  clustering_config = setup_KMeans(k = 3L),
  outdir = "results/"
)
}
\author{
EDG
}
