% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_linad.R
\name{draw_linad}
\alias{draw_linad}
\title{Plot a Linear Additive Tree}
\usage{
draw_linad(
  x,
  n_leaves = NULL,
  top = NULL,
  sort_coefs = TRUE,
  direction = "UD",
  node_labels = TRUE,
  show_ncases = TRUE,
  show_node_value = TRUE,
  lo_col = "#0290EE",
  mid_col = "#1A1A1A",
  hi_col = "#FE4AA3",
  root_col = "#7F7F7F",
  node_col = "#404040",
  leaf_col = "#16A0AC",
  theme = choose_theme(getOption("rtemis_theme")),
  verbosity = 1L
)
}
\arguments{
\item{x}{\code{Regression} or \code{Classification} object holding a LINAD model, or a
fitted LINAD model itself.}

\item{n_leaves}{Optional Integer: Draw the tree at this size. NULL draws the
size selected at training.}

\item{top}{Optional Integer: Show only this many coefficients per node,
largest in absolute value first.}

\item{sort_coefs}{Logical: If TRUE, order each table by absolute coefficient.}

\item{direction}{Character \{"UD", "DU", "LR", "RL"\}: Tree direction.}

\item{node_labels}{Logical: If TRUE, label nodes with their rule.}

\item{show_ncases}{Logical: If TRUE, include the case count in node labels.}

\item{show_node_value}{Logical: If TRUE, include the node's own value in its
label.}

\item{lo_col}{Character: Color for the most negative coefficient.}

\item{mid_col}{Character: Color at zero.}

\item{hi_col}{Character: Color for the most positive coefficient.}

\item{root_col}{Character: Root node color.}

\item{node_col}{Character: Internal node color.}

\item{leaf_col}{Character: Terminal node color.}

\item{theme}{\code{Theme} object.}

\item{verbosity}{Integer: If > 0, print messages.}
}
\value{
\code{visNetwork} htmlwidget.
}
\description{
Draw a fitted LINAD model as an interactive tree: the partition as a
hierarchy, each node's value on its label, and each node's linear
coefficients in a table on hover.
}
\details{
The coefficient tables are the point. A LINAD leaf's coefficients are the
accumulated sum along its path, so the same feature can carry a different
slope in each region of the data -- and sometimes a different \strong{sign}. That
is the model's central interpretive claim, and it is only visible by
comparing the table at one node against the table at another. The color
scale is therefore
diverging and centered on zero, and computed once across every table, so a
sign change reads as a color flip rather than as a number to be parsed.

Each node's label carries \code{node_value}, what the \strong{tree alone} predicts
there, while the table on hover carries what the \strong{linear model} adds. The
two are separate quantities in a fitted LINAD, so the plot shows them
separately: with \code{node_model = "constant"} the tables are empty of slopes and
the node values are the entire model, which is what a plain decision tree
looks like.

The intercept is excluded from the color range. It is typically far larger
than any slope and would compress every other coefficient into the middle of
the scale.

This is the reference implementation for the LINAD tree plot, built on
\code{visNetwork} because a tooltip carrying a real HTML table needs a backend
whose tooltip is an HTML element. Plotly, which every other \verb{draw_*} uses,
renders hover labels as SVG and cannot express a table at all.
}
\examples{
\dontshow{if (interactive() && requireNamespace("visNetwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
x <- data.frame(a = rnorm(300), b = rnorm(300))
x$y <- 2 * x$a + ifelse(x$b < 0, -3, 3) + rnorm(300)
mod <- train(x, hyperparameters = setup_LINAD(max_leaves = 5L))
draw_linad(mod)
\dontshow{\}) # examplesIf}
}
\author{
EDG
}
