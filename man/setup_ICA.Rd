% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/150_DecompositionConfig.R
\name{setup_ICA}
\alias{setup_ICA}
\title{setup_ICA}
\usage{
setup_ICA(
  k = 3L,
  type = "parallel",
  fun = "logcosh",
  alpha = 1,
  row_norm = TRUE,
  maxit = 100L,
  tol = 1e-04,
  features = NULL
)
}
\arguments{
\item{k}{Integer [1, Inf): Number of components.}

\item{type}{Character \{"parallel", "deflation"\}: Type of ICA.}

\item{fun}{Character \{"logcosh", "exp"\}: ICA function.}

\item{alpha}{Numeric [1, 2]: Used in approximation to neg-entropy with \code{fun = "logcosh"}.}

\item{row_norm}{Logical: If TRUE, standardize each case across features
before ICA, i.e. subtract each case's mean and divide by its standard
deviation. Appropriate when the features share units and each case's overall
level and scale are a nuisance rather than signal, as for spectra or assays
whose total intensity varies by sample -- the usual situation for the wide,
single-instrument matrices decomposition is run on, which is why it is the
default. It spends two degrees of freedom per case, so its cost is roughly
\code{2 / ncol(x)}: negligible for hundreds of features, but substantial for a
handful, where each case's level and scale are more likely to be signal.
Set FALSE for narrow or mixed-unit data.}

\item{maxit}{Integer [1, Inf): Maximum number of iterations.}

\item{tol}{Numeric [0, Inf): Tolerance.}

\item{features}{Optional Character vector: Names of at least 2 distinct
feature columns to decompose. \code{NULL} decomposes all numeric features.}
}
\value{
ICAConfig object.
}
\description{
Setup ICA config.
}
\examples{
ica_config <- setup_ICA(k = 3L)
ica_config
}
\author{
EDG
}
