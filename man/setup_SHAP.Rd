% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250_Explanation.R
\name{setup_SHAP}
\alias{setup_SHAP}
\title{Setup SHAP Explanation}
\usage{
setup_SHAP(
  estimator = "auto",
  perturbation = NULL,
  scale = NULL,
  background_n = NULL,
  n_coalitions = NULL,
  approach = NULL,
  seed = NULL
)
}
\arguments{
\item{estimator}{Character \{"auto", "exact", "kernel"\}: Which estimator to
use.}

\item{perturbation}{Optional Character \{"interventional", "conditional"\}:
Value function. NULL uses the resolved estimator's documented default.}

\item{scale}{Optional Character \{"margin", "probability"\}: Scale the
contributions are additive on. NULL sets it from the outcome type.}

\item{background_n}{Optional Integer [1, Inf): Cases to subsample from the
background data. NULL uses all of it.}

\item{n_coalitions}{Optional Integer [2, Inf): Most feature subsets the
kernel estimator evaluates. NULL lets the backend decide.}

\item{approach}{Optional Character \{"empirical", "gaussian", "copula",
"ctree", "vaeac", "categorical", "timeseries"\}: Conditional-distribution
estimator used by the kernel estimator.}

\item{seed}{Optional Integer [0, Inf): Random seed.}
}
\value{
\code{SHAPConfig} object.
}
\description{
Configure per-case Shapley additive explanations for \code{explain()}.
}
\details{
Three choices here are semantics rather than tuning, and each is recorded on
the returned explanation so that two of them can never be silently compared.

\strong{\code{perturbation} is the question being asked.} \code{"interventional"} breaks the
dependence between features, attributing to what the model \emph{uses}: a feature
the model ignores gets zero even if it predicts the outcome perfectly.
\code{"conditional"} respects the joint distribution, attributing to what the
case's features \emph{tell you}, so a correlated proxy for a used feature receives
credit. Neither is the correct one -- debugging a model wants the first,
reasoning about a subject wants the second.

\strong{\code{scale} decides whether the contributions add up.} They are additive on
the scale they are computed on. For regression that is the outcome and there
is nothing to choose. For classification the model's additive scale is the
margin, and probability is a nonlinear transform of it, so contributions on
the probability scale do \strong{not} sum to the predicted probability.

\strong{\code{estimator} is normally best left alone.} \code{"auto"} uses the exact
estimator wherever the algorithm has one and the model-agnostic kernel
estimator otherwise; \code{explanation_methods()} reports which applies where.

\code{approach} and \code{n_coalitions} apply only to the kernel estimator, and
\code{approach} only when \code{perturbation} is \code{"conditional"} -- the interventional
answer is not one of several conditional estimators, it is the absence of
conditioning.

Setting either therefore requires \code{estimator = "kernel"} rather than
\code{"auto"}, even for an algorithm \code{"auto"} would send to the kernel estimator
anyway. Tuning an estimator is a decision to use it, and the alternative --
accepting settings that a resolved exact estimator would then ignore --
would drop them silently.
}
\examples{
setup_SHAP()
setup_SHAP(perturbation = "conditional")
setup_SHAP(estimator = "kernel", perturbation = "conditional", approach = "ctree")
}
\author{
EDG
}
