% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/120_ClusteringConfig.R
\name{setup_PAM}
\alias{setup_PAM}
\title{Setup PAMConfig}
\usage{
setup_PAM(
  k = 3L,
  dist = "euclidean",
  stand = FALSE,
  do_swap = TRUE,
  variant = "original",
  nstart = NULL
)
}
\arguments{
\item{k}{Integer [1, Inf): Number of clusters. Must be fewer than the number of cases.}

\item{dist}{Character \{"euclidean", "manhattan"\}: Distance measure to use.}

\item{stand}{Logical: If TRUE, standardize each variable before computing dissimilarities.}

\item{do_swap}{Logical: If TRUE, perform the swap phase.}

\item{variant}{Character \{"original", "o_1", "o_2", "f_3", "f_4", "f_5", "faster"\}: Algorithm variant; "original" is the published algorithm, the others trade exactness for speed.}

\item{nstart}{Optional Integer [1, Inf): Number of random starts. NULL uses the deterministic build phase instead of drawing the initial medoids at random.}
}
\value{
\code{PAMConfig} object.
}
\description{
Setup a \code{PAMConfig} object for Partitioning Around Medoids (PAM) clustering,
via the 'cluster' package.
}
\details{
PAM is the medoid-based counterpart of k-means: each cluster is represented
by one of the cases rather than by a mean, which makes it usable with
non-Euclidean dissimilarities and less sensitive to outliers.

Argument names are rtemis' own. They map to \code{cluster::pam()} as:
\code{dist} -> \code{metric}, \code{do_swap} -> \code{do.swap}; the rest keep their names.
}
\examples{
pam_config <- setup_PAM(k = 3L)
pam_config
}
\references{
Kaufman L, Rousseeuw PJ (1990). \emph{Finding Groups in Data: An
Introduction to Cluster Analysis}. Wiley.
\doi{10.1002/9780470316801}
}
\author{
EDG
}
