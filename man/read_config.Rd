% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_config.R
\name{read_config}
\alias{read_config}
\title{Read an rtemis config from file}
\usage{
read_config(file, kind = NULL)
}
\arguments{
\item{file}{Character: Path to input JSON config file.}

\item{kind}{Character or NULL: Force the config family (\code{"supervised"},
\code{"decompose"}, \code{"cluster"}, \code{"decomposition"}, or \code{"clustering"}) instead of
detecting it from \verb{$schema}.}
}
\value{
A \code{SuperConfig}, \code{DecomposeConfig}, \code{ClusterConfig},
\code{DecompositionConfig}, or \code{ClusteringConfig} object.
}
\description{
Read a schema.rtemis.org JSON config and return the appropriate rtemis object.
Two families of config are supported: pipeline recipes that bundle a data
path, algorithm config, and output directory (\code{SuperConfig},
\code{DecomposeConfig}, \code{ClusterConfig}), and the bare algorithm configs they wrap
(\code{DecompositionConfig}, \code{ClusteringConfig}). The file is JSON, as written by
\link{write_config} and consumed by rtemislive and the \code{rtemis} CLI.
}
\details{
The config family is taken from the instance's \verb{$schema}; pass \code{kind} to
override (a config without a \verb{$schema} defaults to \code{supervised}).
}
\examples{
# Create a SuperConfig object
x <- setup_SuperConfig(
  dat_training_path = "~/Data/iris.csv",
  algorithm = "LightRF",
  hyperparameters = setup_LightRF()
)
# Write JSON config file
tmpdir <- tempdir()
tmpfile <- file.path(tmpdir, "rtemis_test.json")
write_config(x, tmpfile, overwrite = TRUE)
# Read config back from JSON file
x_read <- read_config(tmpfile)
}
\author{
EDG
}
