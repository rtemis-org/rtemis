% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/090_Preprocessor.R
\name{.list_to_PreprocessorConfig}
\alias{.list_to_PreprocessorConfig}
\title{Convert a list to a PreprocessorConfig object}
\usage{
.list_to_PreprocessorConfig(x)
}
\arguments{
\item{x}{Named list of \code{setup_Preprocessor} parameters.}
}
\value{
A \code{PreprocessorConfig} object.
}
\description{
Internal function used to reconstruct a \code{PreprocessorConfig} object from a
named list, such as the result of parsing a JSON config conforming to the
schema.rtemis.org preprocessor schema. Elements are passed to
\link{setup_Preprocessor}; document metadata (\verb{$schema}) is dropped first, since
it identifies the document rather than naming a parameter.
}
\details{
The learned values (\code{scale_centers}, \code{scale_coefficients}, \code{one_hot_levels},
\code{factor2integer_levels}) are \code{data_dependent} settable inputs: a
user-authored config typically carries none, and the \verb{setup_*} defaults leave
them unset. A config that does carry them -- a fitted preprocessor written
back out -- restores them, which is why they are serialized at all (see
"Property roles" in \verb{010_Props.R}).
}
\examples{
.list_to_PreprocessorConfig(list(scale = TRUE, center = TRUE))
}
\author{
EDG
}
\keyword{internal}
