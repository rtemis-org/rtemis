% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/070_Hyperparameters.R
\name{setup_KNN}
\alias{setup_KNN}
\title{Setup KNN Hyperparameters}
\usage{
setup_KNN(k = 7L, kernel = "optimal", distance = 2, scale = TRUE, ifw = FALSE)
}
\arguments{
\item{k}{(Tunable) Integer [1, Inf): Number of neighbors. Must be less than the number of training cases.}

\item{kernel}{(Tunable) Character \{"rectangular", "triangular", "epanechnikov", "biweight", "triweight", "cos", "inv", "gaussian", "rank", "optimal"\}: Kernel used to weight neighbors by distance.}

\item{distance}{(Tunable) Numeric (0, Inf): Parameter of the Minkowski distance.}

\item{scale}{Logical: If TRUE, scale features to unit variance before computing distances.}

\item{ifw}{(Tunable) Logical: If TRUE, use Inverse Frequency Weighting in classification.}
}
\value{
KNNHyperparameters object.
}
\description{
Setup hyperparameters for k-Nearest Neighbors training.
}
\details{
Both outcome types are fit with \link[kknn:train.kknn]{kknn::train.kknn}, which selects between
regression and classification from the outcome. Factors are one-hot encoded
by rtemis first, as an algorithm-internal preprocessor that is re-applied at
predict time: the backend's own factor handling resolves a contrast function
off the search path, which is not reachable when \code{kknn} is loaded but not
attached, as a suggested package is.

\code{kknn} provides no case weights, so \code{ifw} cannot be honored: enabling it
makes training abort rather than silently fit an unweighted model.
}
\examples{
knn_hyperparams <- setup_KNN(k = 11L, kernel = "rectangular")
knn_hyperparams
}
\author{
EDG
}
